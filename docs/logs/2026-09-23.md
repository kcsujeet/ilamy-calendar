# Development Log - 2026-09-23

## Changes

- **[agents]**: The posting section of `.agents/review-conventions.md` now describes both guards on GitHub writes. kc-claude-kit code-review 2.0.0 ships its own PreToolUse hook, which blocks review writes without `KC_REVIEW_POST_APPROVED=1`. This repo's `check-pr-post-approval.sh` still blocks every public `gh` post without the `touch .claude/state/pr-post-approved.flag` marker. A review comment now needs both, and the example command shows them together.
- **[agents]**: Posting now drafts with `/code-review:post-review`, which validates each comment's file and line against the diff before printing the commands to approve.

## Files Modified

- `.agents/review-conventions.md` - posting section rewritten for the two guards

## Notes

- This repo's hook is kept on purpose: it also covers `gh pr create` and `gh issue create`, which the AGENTS.md hard rule requires approval for, and the kit's guard does not.
- The kit plugins were updated on this machine: code-review 2.0.0 at user and project scope, conventions 1.1.0 at user and local scope. Moving code-review from 1.x to 2.0.0 did not install its new `conventions` dependency; outside this repo it stayed disabled with a dependency error until `claude plugin install conventions@kc-claude-kit` ran.
- The conventions plugin's SessionStart hook reports "not installed" here, because this repo keeps its own `.agents/rules/` and has not run `/conventions:init`. That is expected.
