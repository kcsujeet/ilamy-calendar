# Development Log - 2026-05-17

## Changes

- **[skill/code-review]**: Rewrote `.claude/skills/code-review/SKILL.md` (formerly 108 lines, now 315) to encode session learnings. Adds Phase 0 scope check for stacked branches and title-vs-scope mismatches, Conventional Comments format for drafts (subject + blank line + body), no em dashes, no pleasantries, project readability conventions (colocation, named multi-clause conditionals, no multi-level ternaries, no nested if/switch, no ternaries in JSX), default-to-no-decoration rubric (only ask user when severity is genuinely ambiguous, batch asks), and Phase 5 posting decision tree (individual inline comments via `/pulls/N/comments` when no top-level body is needed, `REQUEST_CHANGES`/`COMMENT` review only when a real structural observation exists). API shapes verified via WebFetch this session.
- **[review-only output]**: Posted reviews to GitHub on PRs #122, #124, and stacked-branch one-liners on #125–#131. None of those changes touched this repo's `src/`; only the skill update and this log do.

## Files Modified

- `.agents/skills/code-review/SKILL.md` — full rewrite (see Changes section).
- `docs/logs/2026-05-17.md` — this file.

## Notes

- `.claude/skills/` symlinks to `.agents/skills/`, so git tracks the path as `.agents/skills/code-review/SKILL.md`.
- The dev-log hook (`.claude/hooks/check-dev-log.sh`) fires when `src/` mtime is newer than today's log. `git checkout` between PR branches during reviews updates mtimes as a side effect, even when no source files actually changed. Touching the log is enough to satisfy it in that case.
- Memory was also updated outside the repo: `feedback_code_style.md` expanded from 4 to 12 rules to cover colocation, named vars, multi-level ternaries, nested if/switch, JSX ternaries, and "no incomprehensible code"; `feedback_review_style.md` added covering Conventional Comments format and the decoration rubric.
