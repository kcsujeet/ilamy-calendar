# Development Log - 2026-09-20

## Changes

- **[drag and drop] refactor**: Cleared the two Fallow findings the CI gate
  raised on PR #283 (`--gate new-only`, 3 error-severity findings in changed
  files).
  - `GridAxis` was exported with no consumer outside its own module — the two
    providers pass the axis as a literal and `useGridAxis` returns it. Un-exported.
  - `DroppableCell` breached the cognitive-complexity threshold (20) at 110
    lines. The droppable registration plus the highlight decision moved into a
    `useDropTarget` hook, and the tint into a `DropTargetHighlight` component,
    leaving the cell's body about being a cell.

- **[drag and drop] docs**: Corrected a comment that my own change had made
  false. It claimed a disabled cell "is not registered as a droppable and
  carries `pointer-events-none`, so `isOver` never fires for one" — true before
  the previous commit, false after it, since disabled cells register now
  precisely so the mirror keeps rendering over them.

## Files Modified

- `packages/calendar/src/contexts/grid-axis-context.tsx` - `GridAxis` no longer exported
- `packages/calendar/src/components/droppable-cell.tsx` - `useDropTarget` + `DropTargetHighlight` extracted; stale comment corrected

## Notes

- **Run the Fallow gate BEFORE opening a PR, not after.** `AGENTS.md` documents
  the command and I skipped it, so CI caught what one local command would have.
  `bunx fallow@3.20.0 audit --changed-since main --gate new-only --no-cache`
- The two remaining styling notes (`border-[1.5px]`, `text-[10px]`) are
  advisory-only (`rules.css-token-drift=warn`) and do not gate. Both are moved
  code rather than new drift — `default-event-content.tsx` was extracted from
  `draggable-event.tsx` — but Fallow reads any line new to the diff as
  introduced. Worth a scale token eventually; out of scope here.
- **`bun run e2e` finally ran on this machine: 39 passed**, including all six
  drag specs and the three rewritten blind earlier in the branch. The Chromium
  launch abort (`mach_port_rendezvous_mac.cc:159`) that blocked it all of
  yesterday is gone.
- Gates: `bun run ci` exit 0 (995 calendar tests), `bun run e2e` 39 passed,
  Fallow `✓ No issues in 45 changed files`.
