# Development Log - 2026-05-16

## Commits (landed)

- `4d62f8c` — `refactor(i18n): format header and view dates with Intl`
- `952b58b` — `chore(i18n): remove unused month and long weekday translation keys`
- `bc61f96` — `refactor(i18n): format weekday labels with Intl and drop sun–sat keys`
- `139a6be` — `feat(i18n): localize DatePicker and Calendar with Intl and locale context`
- `bc8cdd2` — `feat(week-view): align header, all-day row, and time grid columns`
- `0a481d8` — `fix: alignment in allday cell and width of hour column in week-view`
- `60ccebe` — `fix: alignment of gutter columns in week view`
- `300a7d4` — `style: small fixe on border on DayWiew`
- `634ddd4` — `feat(recurrence): presets, full RRULE editor, and aligned demo events`

## Changes (pending commit)

- **[event-form]**: Moved `EventFormTextField` to module scope so title/description/location inputs keep focus while typing (nested component was remounting on each keystroke).

## Changes (pending commit — recurrence)

- **[recurrence-editor / yearly layout]**: In customize mode, yearly month moves to the frequency row:
  `repeatsEvery` + interval + year + `recurrenceOn` + month select (`RecurrenceMonthSelect`).
  On day / on the below reuses `MonthlyRecurrenceFields` (`testIdPrefix="yearly"`).
- **[monthly-recurrence-fields]**: Exported `RecurrenceMonthSelect` and `MONTH_NUMBERS` for shared month dropdown.
- **Removed** `yearly-recurrence-fields.tsx` (duplicate UI; ~207 lines).
- **Tests**: Yearly expects `yearly-on-month-select` in freq row only; no `yearly-on-the-month-select`.

## Changes (full day — recurrence)

- **[recurrence-editor]**: Preset select (Once, daily, weekly on …, customize), full customize RRULE UI,
  `referenceDate`. Intl for weekday/month labels (`formatLocaleWeekday`, `formatLocaleMonth`,
  `getOnTheDaySelectionLabel`).
- **[recurrence utils]**: `recurrence-preset-utils.ts`, `rrule-editor-utils.ts` + tests;
  `weekly-recurrence-fields`, `monthly-recurrence-fields`.
- **[translations]**: Recurrence preset/UI keys (`recurrenceOnce`, `onDay`, `first`, …); **93** keys
  (calendar date names still via `Intl`).
- **[event-form]**, **[radio-group]**, **[seed/demo]**: Wired to new recurrence editor.

## Changes (full day — i18n)

- Header, views, DatePicker use `Intl` + `locale`; `pickADate`, `previousMonth`, `nextMonth` keys.
- Removed month + weekday name translation keys (38).

## Changes (full day — week view layout)

- Column alignment for header, all-day row, time grid; gutter and hour-column fixes.

## Files Modified (pending commit)

- `src/features/recurrence/components/recurrence-editor/recurrence-editor.tsx`
- `src/features/recurrence/components/recurrence-editor/monthly-recurrence-fields.tsx`
- `src/features/recurrence/components/recurrence-editor/recurrence-editor.test.tsx`
- `src/features/recurrence/components/recurrence-editor/yearly-recurrence-fields.tsx` (deleted)

## Files Modified (`634ddd4` and earlier)

- `src/features/recurrence/components/recurrence-editor/*`, `src/features/recurrence/utils/*`
- `src/components/event-form/event-form.tsx`, `event-form.test.tsx`
- `src/components/ui/radio-group.tsx`, `date-picker.tsx`, `calendar.tsx`
- `src/lib/utils/date-locale-format.ts`, `src/lib/translations/*`
- `src/features/calendar/components/week-view/*`, header/views i18n files

## Notes

- `RecurrenceEditor` requires `CalendarProvider`; preset labels use `recurrence*` keys (not `sun`/`january`).
- Recurrence-editor tests: **74** pass after yearly layout change.
