# Development Log - 2026-05-16

## Commits (landed)

- `4d62f8c` — `refactor(i18n): format header and view dates with Intl`
- `952b58b` — `chore(i18n): remove unused month and long weekday translation keys`
- `bc61f96` — `refactor(i18n): format weekday labels with Intl and drop sun–sat keys`

## Changes (pending commit)

- **[date-picker]**: `useSmartCalendarContext` for `currentLocale`, `firstDayOfWeek`, `t`; optional `locale` / `firstDayOfWeek` prop overrides. Trigger label via `formatLocaleDate` (`month: short`, `day`, `year`); empty state `t('pickADate')` or `label` prop. Removed `dayjs(...).format('MMM D, YYYY')`.
- **[calendar]**: Weekday headers from `getWeekDays` + `formatLocaleDate(..., { weekday: 'short' })`; month caption `{ month: 'long', year: 'numeric' }`. Dropped hardcoded `WEEKDAY_LABELS` (`S`/`M`/…). Nav `aria-label`s via `previousMonth` / `nextMonth` props.
- **[translations]**: Added `pickADate`, `previousMonth`, `nextMonth` (**73** keys total).
- **[event-form test]**: `formats date picker labels with the calendar locale` (`fr-FR`).
- **[docs]**: `translation-usage.md` date-picker keys; `AGENTS.md` / `load-context.md` → 73 keys.

## Changes (full day)

- **[i18n / Intl]**: Replaced translation-key date assembly in header and views with `Intl.DateTimeFormat` via `formatLocaleDate` and `formatLocaleDateRange` (restores intent of `day.format('dddd, MMM D')` removed in 8ed6035).
- **[title-content]**: Day/week/month picker labels and popover titles use `currentLocale`.
- **[day-view]**: Header uses long weekday/month + numeric day/year via `formatLocaleDate`.
- **[year-view]**: Month titles use `{ month: 'long' }` via `formatLocaleDate`.
- **[month-header / week-view / recurrence-editor]**: Weekday labels use `formatLocaleDate` / `getOrderedWeekdayInitials` instead of `t('sun')`…`t('sat')`.
- **[date-locale-format]**: Added `formatLocaleDate`, `formatLocaleDateRange`, `getDateForWeekdayIndex`, `formatLocaleWeekday`, `formatLocaleWeekdayInitial`, `getOrderedWeekdayInitials`; removed `MONTH_KEYS`, `MONTH_SHORT_KEYS`, `WEEKDAY_KEYS`, `isDayFirstLocale`, `formatDayViewHeaderDate`.
- **[translations]**: Removed month keys (24), long weekdays (7), short weekdays `sun`–`sat` (7). All calendar date *names* use `locale` + `Intl`.
- **[dev logs]**: Canonical `docs/logs/2026-05-15.md`; removed split `2026-05-15-translation.md` and oldest log `2026-04-12.md` (10-file cap).

## Files Modified (pending commit)

- `src/components/ui/date-picker.tsx`, `calendar.tsx`
- `src/components/event-form/event-form.test.tsx`
- `src/lib/translations/default.ts`, `types.ts`
- `docs/translation-usage.md`, `AGENTS.md`, `.agents/commands/load-context.md`, `docs/logs/2026-05-16.md`

## Files Modified (commits 4d62f8c → bc61f96)

- `src/lib/utils/date-locale-format.ts`, `date-locale-format.test.ts`
- `src/components/header/title-content.tsx`, `base-header.test.tsx`
- `src/features/calendar/components/day-view/day-view.tsx`, `day-view.test.tsx`
- `src/features/calendar/components/year-view/year-view.tsx`, `year-view.test.tsx`
- `src/features/calendar/components/month-view/month-header.tsx`, `month-view.test.tsx`
- `src/features/calendar/components/week-view/week-view.tsx`, `week-view.test.tsx`
- `src/features/recurrence/components/recurrence-editor/recurrence-editor.tsx`
- `src/lib/translations/default.ts`, `types.ts`
- `docs/translation-usage.md`, `AGENTS.md`, `docs/logs/2026-05-15.md`, `docs/logs/2026-05-16.md`

## Notes

- Event-form `DatePicker` was the last English-only UI surface; pending commit completes locale parity with headers and grid views.
- `DatePicker` must render inside `CalendarProvider` / `ResourceCalendarProvider` (uses `useSmartCalendarContext`).
- MediWay: `my-agenda` translator synced separately — removed month/weekday keys, added `pickADate` / `previousMonth` / `nextMonth` + Lingui `fr` msgstr.
- Pending commit: `feat(i18n): localize DatePicker and Calendar with Intl and locale context`
- `bun run ci` green after date-picker work: **836** tests, build **593 KB** raw / **164 KB** gzip.
