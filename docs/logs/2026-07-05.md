# Development Log - 2026-07-05

## Changes

- **[docs/website]**: Added a v2.0.0 version requirement note to the recurrence plugin page's opt-in callout, and two FAQ troubleshooting entries: plugin subpaths not resolving on 1.x, and how to make scoped recurring edits (`applyScopedEdit`/`applyScopedDelete`) from a custom event form. Prompted by a user support email — they were on 1.8.1 reading the v2 docs and couldn't import `@ilamy/calendar/plugins/recurrence` (the subpath only exists from v2.0.0), and had no public API for scoped edits (1.x's `useIlamyCalendarContext()` deliberately omitted the recurring mutators).

## Files Modified

- `apps/website/src/content/docs/plugins/recurrence.mdx` - Added "requires v2.0.0 or later" sentence with migration-guide link to the existing opt-in caution
- `apps/website/src/content/docs/help/faq.mdx` - Added two Troubleshooting entries: plugin subpath import failing on 1.x, and scoped recurring edits from a custom form

## Notes

No `src/` changes — website docs only. Verified against `packages/calendar/package.json` exports (all three plugin subpaths are v2-only) and the current `useIlamyCalendarContext()` return shape (`applyScopedEdit`, `applyScopedDelete`, `getEventManager` are public in v2). Site not yet rebuilt/deployed; docs deploy happens with the next release or on request.
