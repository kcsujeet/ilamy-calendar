# Development Log - 2026-05-18

## Changes

- **[issue-138 / slotDuration]**: Added `slotDuration?: 15 | 30 | 60` prop on `IlamyCalendar` and `IlamyResourceCalendar`. Default `60` (hour-only, no sub-hour lines). Matches FullCalendar's `slotDuration` naming; values restricted to quarter-hour increments per user preference. Threaded through `CalendarProvider`/`ResourceCalendarProvider` into context; views (`day-view`, `week-view`, `resource-day-vertical`, `resource-week-vertical`) read from context and forward to `VerticalGrid`. Internal grid prop renamed from `cellSlots: number[]` (array of minute offsets) to `slotDurationMinutes: number`. Removed the hardcoded `cellSlots={[0, 15, 30, 45]}` in `day-view.tsx` and `resource-day-vertical.tsx`. New `SlotDuration` type re-exported from `src/index.ts`.
- **[tests]**: Added three day-view tests (default hour-only, `slotDuration=30`, `slotDuration=15`), one resource-day-vertical test (default hour-only), two week-view tests (default hour-only, `slotDuration=15`), and two resource-week-vertical tests (default hour-only, `slotDuration=15`). Updated `ilamy-calendar.test.tsx` day-view `onCellClick` test and resource-day-vertical `renders 15-minute slots` test to pass `slotDuration={15}` explicitly. Renamed the existing `verifies 15-minute time slot granularity` and `applies styling at exact boundary times` tests to opt into `slotDuration: 15`.
- **[self-review cleanup]**: Replaced the `[60]` sentinel and `isQuarter` misnomer in `vertical-grid-col.tsx` with three named locals: `slotCount` (factor of 60), `cellOffsets` (the offsets array derived from `Array.from`), and `hasSubHourSlots` (a `cellOffsets.length > 1` check). The dashed-border and minute-attribute decisions read `hasSubHourSlots` instead of `minute !== 60`. Eliminates the multi-line ternary and reads top-to-bottom. Behavior unchanged; same tests pass.
- **[demo page]**: Added a `Slot Duration` select control to the demo settings sidebar (`demo-calendar-settings.tsx`) and wired it through `demo-page.tsx` to both `IlamyCalendar` and `IlamyResourceCalendar`. Default 60 min; options 60/30/15.
- **[docs]**: `docs/types-and-interfaces.md` props table updated with `slotDuration` row. `docs/time-grid.md` updated to describe `slotDurationMinutes` instead of `cellSlots`. `docs/api-parity-checklist.md` marks `slotDuration` as parity-complete.
- **[review-only output]**: Replied to maxdelorme on PR #136 (clarified scrollTime scope vs his fork branch), posted REQUEST_CHANGES on PR #135 (no linked issue or repro steps).
- **[merge main]**: Merged origin/main into `feat/slot-duration-prop` after PRs #136 (scrollTime) and #137 (cell-text-overflow) landed. Seven conflicts, all "both branches added a new prop / context field / state": resolved by keeping both. Files: `IlamyCalendarProps`, `CalendarContextType`, `CalendarProvider`, `ResourceCalendarProvider`, `DemoCalendarSettings`, `demo-page.tsx`, `docs/logs/2026-05-17.md`. Type-check clean, 854 tests pass after merge.
- **[release]**: Cut v1.7.0 — `scrollTime` (#136), `slotDuration` (#140), horizontal now-line (#122, by @sumanthneerumalla), cell-overflow truncation (#137), demo-page GPU fix (#127, by @maxdelorme). Bundle: 599 KB raw / 166 KB gzip. Default flip in day-view from 15-min to 60-min slots called out in the changelog.
- **[fix/sync-resources-prop / #153]**: Three-part fix.
  1. Dropped `useState` snapshot of the `resources` prop in `ResourceCalendarProvider` — it was capturing the initial value and ignoring subsequent updates. Now reads the prop directly.
  2. Audited the resource-visibility API (`visibleResources`, `getVisibleResources`, `showResource`, `hideResource`, `toggleResourceVisibility`, `showAllResources`, `hideAllResources`) and confirmed it was dead scaffolding: zero callers outside the provider itself, and not exposed in the public API (`src/index.ts`). Deleted the whole system. All 5 resource views now read `resources` from context directly instead of `getVisibleResources()`. Provider drops from 330 → 274 lines.
  3. Cleaned up the `CustomResourceEventForm` test fixture in `ilamy-resource-calendar.test.tsx`: dropped two `as CalendarEvent` casts and one `as CalendarEvent | null`, replaced three `!` non-null assertions with a `{selectedEvent && ...}` guard around the update/delete buttons, added `type="button"` on each button, dropped the unused `close-form-btn`, dropped the 9 useless `renderEventForm={(props) => <CustomResourceEventForm {...props} />}` wrapper closures (now just `renderEventForm={CustomResourceEventForm}`).
  4. Added an interactive resource picker to the library's demo page (`demo-page.tsx`): the static "Resources" info card is now 6 checkboxes that toggle which `demoResources` get passed to `IlamyResourceCalendar`. `calendarKey` deliberately excludes the picker state so toggling exercises the in-place prop update path. Visual validation for #153 without writing app glue code.
  Three new TDD tests in `ilamy-resource-calendar.test.tsx` covering replace / add / remove. 857 tests, 0 fail.
- **[locale]**: Fixed dayjs locale not applying on mount when `locale` is set (e.g. demo `calendarKey` remount). `lastLocaleProp` is initialized as `undefined` so the locale effect runs once and calls `dayjs.locale()` and `currentDate.locale()`.
- **[i18n]**: Removed unused weekday keys (`sunday`–`saturday`, `sun`–`sat`) and month keys (`january`–`december`) from `Translations` / `defaultTranslations`. Date labels use dayjs `format()` instead of `t()` where the global locale applies.
- **[recurrence]**: Weekly “Repeat on” labels use `weekDay.format('ddd')` (replaces `DAY_KEYS` + `t('sun')`…`t('sat')`).
- **[date-utils]**: Added `isDayBeforeMonth`, `formatWeekdayDayMonth`, `formatShortMonthDay`, `formatWeekPickerRange` (compact when same month; both ends with month when the week crosses a month), and `getWeekdayHeaderLetter` (first letter of `format('dd')`, uppercased).
- **[header / title-content]**: Month/day/week pickers use dayjs helpers with `currentLocale` only where needed (`isDayBeforeMonth` / explicit `day.locale()` for `Intl`-driven ordering).
- **[year-view]**: Mini-calendar weekday headers use shared `getWeekdayHeaderLetter` + `getWeekDays` (replaces hardcoded initials and the local inline helper).
- **[ui/calendar]**: Date-picker popover grid drops `WEEKDAY_LABELS`; uses `getWeekdayHeaderLetter` + `getWeekDays`, relying on global `dayjs.locale()` from `useCalendarEngine` (no `locale` prop on `Calendar`).
- **[ui/date-picker]**: Reads `firstDayOfWeek` via `useSmartCalendarContext` selector (scalar return, not object destructure); passes it to `Calendar`; trigger label uses `format('ll')` instead of hardcoded `MMM D, YYYY`.
- **[day-view]**: Header date uses `format('LL')` via dayjs `localizedFormat` plugin.
- **[types]**: `CalendarContextType.currentLocale` is required (`string`), matching `useCalendarEngine` (fixes CI `string | undefined` errors).

## Files Modified

- `src/hooks/use-calendar-engine.ts` - `lastLocaleProp` initialized to `undefined`; `dayjs.locale()` on mount/change
- `src/hooks/use-calendar-engine.test.ts` - Locale on mount and on prop change
- `src/lib/translations/types.ts` - Dropped unused weekday and month keys
- `src/lib/translations/default.ts` - Dropped unused weekday and month defaults
- `src/features/recurrence/components/recurrence-editor/recurrence-editor.tsx` - `format('ddd')` for repeat-on labels
- `src/features/recurrence/components/recurrence-editor/recurrence-editor.test.tsx` - Updated weekday label assertions
- `src/lib/utils/date-utils.ts` - `getWeekdayHeaderLetter`, `formatWeekPickerRange`, and related locale helpers
- `src/lib/utils/date-utils.test.ts` - Locale ordering, week picker ranges, `getWeekdayHeaderLetter` (global `dayjs.locale()`)
- `src/components/header/title-content.tsx` - Locale-aware month/day/week picker labels
- `src/features/calendar/components/year-view/year-view.tsx` - Shared `getWeekdayHeaderLetter` from date-utils
- `src/components/ui/calendar.tsx` - `getWeekDays` / `getWeekdayHeaderLetter` (no hardcoded `WEEKDAY_LABELS`)
- `src/components/ui/date-picker.tsx` - Context selector for `firstDayOfWeek`; `format('ll')` on trigger; TS fix (selector returns `number`)
- `src/features/calendar/contexts/calendar-context/context.ts` - `currentLocale` required
- `src/lib/configs/dayjs-config.ts` - `localizedFormat` plugin for `LL` / `ll`
- `src/features/calendar/components/day-view/day-view.tsx` - Header uses `format('LL')`
- `src/features/calendar/components/day-view/day-view.test.tsx` - Asserts `LL` header text

## Notes

- Demo `calendarKey` includes `locale`, which remounts `IlamyCalendar` on every locale change. Without applying locale on mount, `dayjs.locale()` never ran because `lastLocaleProp` matched the prop immediately.
- **Breaking for consumers**: custom `translations` objects no longer require weekday or month name keys; use the `locale` prop + dayjs locales for month/day names.
- `getWeekdayHeaderLetter` and grid headers depend on `dayjs.locale()` set by the engine; `getWeekDays(dayjs(), …)` creates instances after that global locale is active.
- `title-content` helpers still take `currentLocale` for `Intl` (`isDayBeforeMonth`) and explicit `day.locale()` where ordering differs from a plain `format()`.
- Week/month grid column headers still use `day.format('ddd')` (dayjs locale).
- `useSmartCalendarContext(selector)` returns the selector’s return type (`T`), not the full context — e.g. `(ctx) => ctx.firstDayOfWeek` yields a `number`, so assign it directly rather than destructuring.
