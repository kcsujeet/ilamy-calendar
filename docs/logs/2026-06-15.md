# Development Log - 2026-06-15

## Changes

- **[issue #205]**: Investigated "Resource view month doesn't highlight current date". The bug is in the reporter's published `1.8.1` (released 2026-06-06); it is already fixed on `main`. The today-highlight in `ResourceMonthHorizontalHeader` was added by commit `c1d562b` (2026-06-13, "share CurrentTimeIndicator + DayLabel via @ilamy/ui"), which post-dates the `v1.8.1` tag — at `v1.8.1` the month view had zero `isToday`/`DayLabel` usage. No production fix needed; the fix ships in the next release.
- **[calendar]**: Added a regression test guarding the resource month (horizontal) today highlight — there was no test for it, which is why it regressed/was-missing. The resource week header was already covered; the month header was not.
- **[ui]**: `DayLabel` now exposes `data-today="true"` on its wrapper when `today` is set, as a stable styling/selection hook (and a non-brittle test target — avoids asserting `bg-primary` classes in JSDOM). `DayLabel` is the single source of truth for today-highlighting, so this hook applies across all views/plugins.
- **[calendar]**: Month view day number now aligns top-left instead of top-center. Passed `className="items-start"` to the grid-cell `DayLabel` (twMerge overrides its default `items-center`). Only the regular month grid sets `showDayNumber`, so other views are unaffected.
- **[website docs]**: Updated docs to v2. Audited each page against `docs/migration-v2.md` and the actual public API (`packages/calendar/src/index.ts`, `IlamyCalendarApi`, types, recurrence/agenda subpath exports). Fixes: plugin opt-in model (`plugins={[recurrencePlugin()]}`), recurrence/agenda subpath imports, `IlamyResourceCalendar` documented as deprecated alias (examples switched to `IlamyCalendar resources={...}`), `IlamyCalendarApi` return-values table corrected (`getEventsForResource`, `Dayjs` types, `view: string`, `rawEvents`, `applyScopedEdit/Delete`, removed `findParentRecurringEvent`/`updateRecurringEvent`/`deleteRecurringEvent`), `Resource.position` removed, `data: Record<string, unknown>`, `Translations` as derived type alias, added a `## Plugins` section to calendar.mdx.
- **[website docs]**: New **Plugins** sidebar section. Moved `features/recurrence.mdx` → `plugins/recurrence.mdx` (now `/docs/plugins/recurrence`) and added a new `plugins/agenda.mdx` documenting `agendaPlugin({ window })` (`AgendaWindow = 'day' | 'week' | 'month' | number`, default `'month'`; resource-incapable). Repointed internal links; old `/docs/features/recurrence` URL intentionally not preserved. Website build passes (24 pages).
- **[skill]**: Updated `.agents/skills/release-new-version/SKILL.md` for the current monorepo. Verified against actual config: bundled-into-`@ilamy/calendar` set now lists `@ilamy/calendar-agenda` and clarifies the plugin packages live under `packages/plugins/`; called out the private-but-NOT-bundled packages (`@ilamy/demo`, `@ilamy/website`, `@ilamy/playground`) and that the docs site deploys separately; documented the four `exports` subpaths; corrected the `bun run ci` definition (`check → build → type-check → test`, root build compiles the apps too); noted `release` = `build:lib` + `publish:lib`.
- **[docs]**: Updated the root `README.md` for v2: removed the broken `import '@ilamy/calendar/dist/index.css'` from Quick Start (the package ships no CSS), added a "Styling — bring your own design system" section (`@source` the package dist) and a "Plugins" section (recurrence + agenda opt-in), and noted recurrence is now a plugin. Mirrors the published `packages/calendar/README.md`.
- **[website]**: Positioned the landing page + SEO around "lightweight, pluggable, ships no CSS". `main-layout.astro`: updated default `<title>`/`description`/`keywords` and the JSON-LD `featureList` (added lightweight ~13 KB gzipped / pluggable / no-CSS; removed the inaccurate "Zero Dependencies Calendar Core"). `index.astro`: hero copy + two new feature cards (Pluggable Architecture, Lightweight) + repurposed the third dev card into a "Plugin System" code example. Icons (`Puzzle`, `Feather`) verified to exist in lucide-react. Website build passes; verified the rendered homepage carries the messaging.
- **[docs]**: Positioned ilamy as "lightweight and highly pluggable" in the root README intro and the website `introduction.mdx`. Measured bundle (verified, not guessed): core `index.js` is 40 KB minified / ~13 KB gzipped; full npm package 213 KB unpacked / 60 KB packed — used the ~13 KB gzipped core as the honest size headline. Also split the README's awkward "Interactions & Globalization" features heading into "Internationalization & Timezones" and "Interactions".
- **[build]**: Made the npm README a single source of truth = the repo-root `README.md` (so the npm page matches the GitHub homepage). `packages/calendar/scripts/prepack.ts` now copies `../../README.md` → `packages/calendar/README.md` at pack time; the package README is untracked + gitignored (generated only at pack), so no postpack restore is needed (npm always ships README regardless of ignore files). Made the root README's `./examples` links absolute so they resolve on npmjs.com. Verified with `npm pack`, `bun pm pack`, and `bun publish --dry-run` that the tarball README equals root (155 lines) and package.json is restored intact.
- **[build]**: Added root `deploy:website` script (`bun run build:lib && bun run --filter '@ilamy/website' deploy`) — mirrors `dev:website` (build lib first so docs render against current `@ilamy/calendar` dist). Documented in `apps/website/README.md`; the release skill's Step 9 now uses it.
- **[skill]**: Renamed the release skill's "Phase N" sections to "Step N" (1–10, all cross-references updated) and added a new **Step 9 — Update and deploy the website**: decide what the release needs on the site (docs usually, sometimes demo/playground), make the changes (verified against source, validated by the website build), then deploy to Cloudflare Pages via `bun run --filter '@ilamy/website' deploy` (manual, needs `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`, explicit approval). Issue-notification step renumbered to Step 10.

## Files Modified

- `packages/ui/src/components/day-label.tsx` - Added `data-today` attribute on the wrapper + doc note on the `today` prop.
- `packages/calendar/src/components/grid-cell.tsx` - Month day number aligned top-left via `items-start` on the `DayLabel`.
- `packages/calendar/src/features/calendar/components/views/month.test.tsx` - New `ResourceMonthHorizontal` regression test: renders the horizontal resource month and asserts exactly one `[data-today="true"]` cell, showing today's day number.
- `apps/website/src/content/docs/{components/calendar,components/resource-calendar,components/use-ilamy-calendar-context,features/internationalization,getting-started/usage}.mdx` - v2 doc updates (see [website docs] above).
- `apps/website/src/content/docs/plugins/recurrence.mdx` - moved from `features/recurrence.mdx`; retitled "Recurrence Plugin"; opt-in plugin + subpath import updates.
- `apps/website/src/content/docs/plugins/agenda.mdx` - new agenda plugin page.
- `apps/website/astro.config.mjs` - new Plugins sidebar group; removed Recurrence from Features.
- `apps/website/src/content/docs/help/support.mdx` - verification-pass fixes (both pre-existing, not v2): broken link `/docs/getting-started/overview` → `/docs/introduction`, and stale GitHub URLs `ilamy-soft/ilamy-calendar` → `kcsujeet/ilamy-calendar` (matches package.json + every other ref).
- `apps/website/src/content/docs/help/faq.mdx` - repointed the recurrence link to `/docs/plugins/recurrence`.

## Notes

- Root-cause method: `git merge-base --is-ancestor c1d562b v1.8.1` returns false (fix not in 1.8.1); `git show v1.8.1` of the month view shows no today-highlight code.
- In the horizontal resource month arrangement only the header renders a today-aware `DayLabel` (body grid cells use `showDayNumber=false`), so exactly one element carries the highlight — the test relies on that invariant.
- Deleted `docs/logs/2026-05-31.md` to stay under the 10-file cap.
