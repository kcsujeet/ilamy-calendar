# Development Log - 2026-06-11

## Changes

- **[v2 phase 0]**: derived Translations type; deleted dead code (month-view types,
  DayNumber.locale, ProcessedCalendarEvent, ui Badge/InputGroup, demo dead entry); data fields
  → unknown; dropped Resource.position. Migration entries added.

## Files Modified

- `packages/calendar/src/lib/translations/default.ts` - Removed `Translations` annotation; object is now the canonical source (`satisfies Record<string, string>`)
- `packages/calendar/src/lib/translations/types.ts` - `Translations` is now derived: `Record<keyof typeof defaultTranslations, string>`
- `packages/calendar/src/features/calendar/components/month-view/types.ts` - Deleted (dead: `MonthViewProps`, `SelectedDayEvents`, `MultiDayEventPosition`, `EventMap`)
- `packages/calendar/src/features/calendar/components/month-view/month-view.tsx` - Dropped dead `MonthViewProps` import/annotation
- `packages/calendar/src/features/calendar/components/ilamy-calendar.tsx` - Stopped threading unread `dayMaxEvents` prop into `MonthView` (cells read it from context)
- `packages/calendar/src/components/day-number.tsx` - Dropped unused `locale` prop
- `packages/calendar/src/components/day-number.test.tsx` - Removed obsolete locale test (prop never affected output)
- `packages/calendar/src/components/grid-cell.tsx` - Stopped passing `locale`; removed now-unused `currentLocale` destructuring
- `packages/calendar/src/components/types.ts` - Deleted unused `ProcessedCalendarEvent` interface
- `packages/ui/src/components/badge.tsx` - Deleted (zero consumers)
- `packages/ui/src/components/input-group.tsx` - Deleted (zero consumers)
- `packages/ui/bunup.config.ts` - Removed badge/input-group build entries
- `apps/demo/src/index.tsx` - Deleted dead Bun-serve entry point (demo runs via Vite)
- `packages/types/src/index.ts` - `CalendarEvent.data` is `Record<string, unknown>` (was `any`)
- `packages/calendar/src/features/resource-calendar/types/index.ts` - `Resource.data` → `Record<string, unknown>`; dropped unused `Resource.position`
- `packages/calendar/src/features/resource-calendar/components/ilamy-resource-calendar/ilamy-resource-calendar.test.tsx` - Removed `position` from fixtures
- `packages/calendar/src/components/resource-cell.test.tsx` - Narrowing guard for `resource.data?.avatar` read
- `apps/demo/src/components/demo/demo-data.ts` - Removed `position` from demo resources
- `docs/migration-v2.md` - New "Type tightening (v2 structure overhaul, Phase 0)" section + checklist items

## Notes

Phase 0 of the v2 overhaul (`docs/v2-overhaul-phase-0-plan.md`): pure subtraction plus type
derivation, no new behavior (TDD deletion exception applies; regression net is the full suite).
One test count change: calendar suite went 767 → 766 because the obsolete `DayNumber` locale
test was removed with the prop. Unplanned type-check fallout from `Resource.position` removal
(demo data + resource-calendar test fixtures) and `data: unknown` (resource-cell test read)
fixed with deletions/narrowing guards, never casts. `bun run ci` green: 154 + 766 pass, 0 fail.

---

# Phase 1 (same day)

## Changes

- **[v2 phase 1]**: unified geometry — `lib/layout/{geometry,vertical,horizontal}` with ONE
  composed `PositionedEvent`; `position`→`row`; renderer owns horizontal pixel math;
  `DraggableEvent` truncation props replace the double-cast; recurrence `omitKeys` hack +
  `@ts-expect-error` deleted; `position-*` strangler shims removed (ProcessedCalendarEvent
  was already deleted in Phase 0).

## Files Modified

- `packages/calendar/src/lib/layout/geometry.ts` - New: the single composed `PositionedEvent` contract (event nested, never spread)
- `packages/calendar/src/lib/layout/vertical.ts` - Moved from `lib/utils/position-day-events.ts`; `layoutVertical` emits the composed shape; split into cluster → geometry → place phases
- `packages/calendar/src/lib/layout/vertical.test.ts` - Moved suite, rewritten for the composed shape (19 → 20 tests; Stability split into reference-identity + no-horizontal-fields pins)
- `packages/calendar/src/lib/layout/horizontal.ts` - Moved from `lib/utils/position-week-events.ts`; `layoutHorizontal` emits `row` + truncation flags, NO pixel fields; `as PositionedEvent` cast gone
- `packages/calendar/src/lib/layout/horizontal.test.ts` - Moved suite, factory helpers, 27 → 19 tests (10 pixel pins retired to the renderer suite, 2 shape pins added)
- `packages/calendar/src/components/horizontal-grid/horizontal-grid-row.test.tsx` - +5 DOM pixel-placement tests covering the retired layout-level pixel assertions
- `packages/calendar/src/components/horizontal-grid/horizontal-grid-events-layer.tsx` - Owns the `top = dayNumberHeight + spacing + row * (eventHeight + spacing)` math; new `dayNumberHeight` prop; passes nested `event` + truncation props to `DraggableEvent`
- `packages/calendar/src/components/horizontal-grid/horizontal-grid-row.tsx` - `dayNumberHeight` re-routed from the hook to the events layer
- `packages/calendar/src/components/vertical-grid/vertical-grid-events-layer.tsx` - Reads the composed shape; `DraggableEvent` receives the clean nested event
- `packages/calendar/src/components/draggable-event/draggable-event.tsx` - Real `isTruncatedStart`/`isTruncatedEnd` props (memo-compared); `as unknown as` double-cast deleted
- `packages/calendar/src/features/calendar/hooks/useProcessedDayEvents.ts` - Calls `layoutVertical`
- `packages/calendar/src/features/calendar/hooks/useProcessedWeekEvents.ts` - Calls `layoutHorizontal`; `dayNumberHeight` prop and `eventSpacing`/`eventHeight` context reads removed (pixel knobs left the hook)
- `packages/recurrence/src/utils/recurrence-handler.ts` - `omitKeys(...)` + `@ts-expect-error` + `as CalendarEvent` replaced by a plain `...targetEvent` spread (events are clean now)
- `packages/calendar/src/lib/utils/index.ts` - Dropped the dead `omitKeys` re-export
- `packages/utils/src/helpers.ts` - Deleted the now-unused `omitKeys`
- `packages/utils/README.md` - Removed the `omitKeys` mention (unplanned but in-spirit)
- `packages/calendar/src/lib/utils/position-day-events.ts` - Deleted (temporary strangler shim)
- `packages/calendar/src/lib/utils/position-week-events.ts` - Deleted (temporary strangler shim)
- `docs/hooks-and-context.md` - Layout function/path references; dropped `dayNumberHeight` from the `useProcessedWeekEvents` example
- `docs/performance.md` - `getPositionedEvents` → `layoutHorizontal` in the render-pass diagram
- `docs/types-and-interfaces.md` - Relationship chain now points at `lib/layout`; stale `ProcessedCalendarEvent` node replaced with `PositionedEvent`
- `.agents/commands/load-context.md` - Key-paths entries for `layout/{geometry,vertical,horizontal}.ts`
- `AGENTS.md` - Key Paths: added `layout/`, trimmed `position-*-events` from `utils/`

## Notes

Phase 1 of the v2 overhaul (`docs/v2-overhaul-phase-1-plan.md`), strangler-pattern execution:
new modules + moved suites first (red→green), shims kept the five consumers byte-identical
until the one-task swap, shims deleted last. Both layout modules stay internal (nothing added
to `src/index.ts`), so no migration-guide entry. Test accounting: calendar 766 → 764
(−27 retired week-layout tests, +19 horizontal, +5 renderer pixel pins, +1 vertical Stability
split); recurrence stays 154. The `data-top`/`data-left` testids and pixel values are
bit-identical (resource suites pass unmodified). `bun run ci` green: 154 + 764 pass, 0 fail.

---

# Phase 2 — One Provider, Engine Slices (v2 overhaul)

## Changes

- **[v2 phase 2]**: split useCalendarEngine into config/navigation/data/interaction slices (timezone+locale effects stay in the composer); deleted ~190 duplicated provider lines via shared useCalendarContextValue; openEventForm carries CellInfo.resource (new public OpenEventFormInput type); resource cell-click respects allDay (breaking, migration entry); SmartCalendarContextType honest (CalendarContextType & Partial<ResourceContextFields>, no cast), public getEventsForResource optional (breaking, migration entry); use-smart-calendar-context moved to features/calendar/hooks.

## Files Modified

- `packages/calendar/src/features/calendar/hooks/use-calendar-config.ts` - NEW: config slice (t, currentLocale state, dayMaxEvents, businessHours)
- `packages/calendar/src/features/calendar/hooks/use-calendar-navigation.ts` - NEW: navigation slice (date/view state, period stepping, range math)
- `packages/calendar/src/features/calendar/hooks/use-calendar-data.ts` - NEW: data slice (event store, prop sync, CRUD, scoped mutations)
- `packages/calendar/src/features/calendar/hooks/use-calendar-interaction.ts` - NEW: interaction slice (selection, event form, handleEventClick/handleDateClick)
- `packages/calendar/src/hooks/use-calendar-engine.ts` - Rewritten as the composer; locale+timezone effects stay here; config gains click props; returns CalendarEngineReturn & CalendarEngineHandlers
- `packages/calendar/src/features/calendar/contexts/calendar-context/provider.tsx` - New shared useCalendarContextValue; CalendarProvider slimmed to context wiring
- `packages/calendar/src/features/calendar/contexts/calendar-context/context.ts` - Declared the `locale?: string` field the value always carried
- `packages/calendar/src/features/resource-calendar/contexts/resource-calendar-context/provider.tsx` - Prop-mapping over useCalendarContextValue (291 → ~115 lines); duplicated handlers/memo deleted
- `packages/calendar/src/features/calendar/types/index.ts` - NEW OpenEventFormInput type (Partial<CalendarEvent> & { resource?: Resource })
- `packages/calendar/src/features/calendar/hooks/use-smart-calendar-context.ts` - Moved from src/hooks/; honest SmartCalendarContextType, no cast; IlamyCalendarApi.getEventsForResource and openEventForm signatures updated
- `packages/calendar/src/index.ts` - Exports OpenEventFormInput; hook export path updated for the move
- `packages/calendar/src/components/grid-cell.tsx` - `getEventsForResource?.(resourceId) ?? []`
- `packages/calendar/src/features/calendar/hooks/useProcessedDayEvents.ts` - same `?.` fix
- `packages/calendar/src/features/calendar/hooks/useProcessedWeekEvents.ts` - same `?.` fix
- `packages/calendar/src/features/resource-calendar/components/{resource-event-grid.tsx,day-view/resource-day-vertical.tsx,day-view/resource-day-horizontal.tsx,month-view/resource-month-vertical.tsx,week-view/use-resource-week-view-data.ts}` - `resources = []` destructure defaults
- `packages/calendar/src/hooks/use-calendar-engine.test.ts` - +2 tests (resource carry, explicit resourceId precedence)
- `packages/calendar/src/features/resource-calendar/components/ilamy-resource-calendar/ilamy-resource-calendar.test.tsx` - +3 provider-fallback tests (CellClickProbe via headerComponent); allDay pin flipped to v2 behavior in Task 6
- ~55 importer files - mechanical `@/hooks/use-smart-calendar-context` → `@/features/calendar/hooks/use-smart-calendar-context` sweep
- `docs/migration-v2.md` - "Provider unification (Phase 2)" section: allDay fallback + optional getEventsForResource; two checklist items
- `docs/hooks-and-context.md`, `docs/v2-plugin-architecture.md`, `AGENTS.md`, `.agents/commands/load-context.md` - Slice composition + new file locations

## Notes

Phase 2 of the v2 overhaul (`docs/v2-overhaul-phase-2-plan.md`). Resource provider behavior was
pinned FIRST (3 tests against v1, including the allDay:false hardcode), then the pin was flipped
failing-first when the provider switched to the engine's unified handleDateClick. One adaptation:
biome's useExhaustiveDependencies rejects a rest-destructured object as a memo dep, so
useCalendarContextValue destructures the handlers off INSIDE the memo and depends on the whole
engine result (same dep granularity as v1). The engine still imports DAY_MAX_EVENTS_DEFAULT-free;
the config slice imports it from `@/lib/constants` (the old engine used the relative
`../lib/constants` path). Tests: calendar 764 → 769 (+3 resource pins, +2 openEventForm);
recurrence stays 154. `bun run ci` green.

---

# Phase 3 — Generalized PluginView Contract, One View-Resolution Path (v2 overhaul)

## Changes

- **[v2 phase 3]**: generalized PluginView (navigationStep/range/columns/layout/renderHeader/supportsResources, all optional); built-ins are PluginView specs resolved through one path; ViewRenderer three-way dispatcher; gutter factory + HEADER_STAGGER_DELAY dedup; year badges read the range pipeline; navigatePeriod consumes navigationStep; ResourceCalendarBody plugin fallback; docs/custom-views.md. Built-ins ship supportsResources: false (flip in Phase 4).

## Files Modified

- `packages/calendar/src/components/vertical-grid/gutter.tsx` - NEW: gutterColumn factory + GUTTER_WIDTH/RESPONSIVE_GUTTER_WIDTH (replaces 5 pasted column literals)
- `packages/calendar/src/lib/constants.ts` - HEADER_STAGGER_DELAY = 0.05 (replaces 7 pasted literals)
- `packages/calendar/src/features/calendar/components/views/view-renderer.tsx` - NEW: three-way dispatcher ('vertical' → VerticalGrid, 'horizontal' → HorizontalGrid, else component); locked engine rule `resources && supportsResources ? orientation : layout`
- `packages/calendar/src/features/calendar/components/views/{day,week,month,year}.tsx` - NEW: built-in PluginView specs (day/week vertical, month horizontal, year escape hatch); day/week/month components are thin ViewRenderer wrappers
- `packages/calendar/src/features/calendar/components/views/index.ts` - NEW: barrel + `builtInViews` array
- `packages/calendar/src/features/calendar/components/views/month-header.tsx` - Moved from month-view/
- `packages/calendar/src/features/calendar/components/views/{day,week,month}.test.tsx` - Moved suites (import-path-only edits); old day-view/, week-view/, month-view/ dirs deleted
- `packages/calendar/src/features/calendar/components/business-hours.test.tsx` - Import paths for moved DayView/WeekView (unplanned fallout)
- `packages/calendar/src/features/calendar/components/ilamy-calendar.tsx` - builtInViews record deleted; CalendarContent resolves only specs via getViews() + ViewRenderer
- `packages/calendar/src/components/vertical-grid/vertical-grid-header-container.tsx` - h-12 → min-h-12 (header nodes own their height; no test pinned the literal)
- `packages/types/src/index.ts` - Resource moved in from calendar; ViewConfig/VerticalColumnSpec/HorizontalCellSpec/HorizontalRowSpec/ColumnSpec/ViewHeaderContext added; PluginView extended (all additions optional)
- `packages/calendar/src/features/resource-calendar/types/index.ts` - Resource → re-export from @ilamy/types; orientation TSDoc rewritten (resource axis vs layout)
- `packages/calendar/src/components/vertical-grid/vertical-grid-col.tsx` - VerticalGridColProps extends VerticalColumnSpec; dead renderHeader field dropped (zero consumers)
- `packages/calendar/src/components/horizontal-grid/horizontal-grid-row.tsx` - HorizontalGridRowProps extends HorizontalRowSpec; HorizontalGridColumn extends HorizontalCellSpec
- `packages/calendar/src/index.ts` + `packages/calendar/src/features/plugins/lib/types.ts` - Re-export the new contract types
- `packages/calendar/src/features/calendar/hooks/use-calendar-navigation.ts` - VIEW_UNITS deleted; calculateViewRange is spec-driven (range ?? getMonthGridRange fallback); getAllViews/resolveViewSpec single resolution path; navigatePeriod consumes navigationStep (else 1 navigationUnit, else 1 day)
- `packages/calendar/src/hooks/use-calendar-engine.ts` - getViews now returns navigation.getAllViews (built-ins prepended)
- `packages/calendar/src/hooks/use-calendar-engine.test.ts` - +2 failing-first tests (navigationStep/range 40-day plugin; getViews prepends built-ins)
- `packages/calendar/src/features/calendar/components/year-view/year-view.tsx` - Badges count via getEventsForDateRange (range pipeline), not raw event.start.month() filtering; `events` context read dropped
- `packages/calendar/src/features/calendar/components/year-view/year-view.test.tsx` - +1 failing-first test (month-boundary-spanning event counted in both badges)
- `packages/calendar/src/lib/utils/date-utils.ts` - NEW getMonthGridRange (6x7 grid range, shared by month spec + engine fallback)
- `packages/calendar/src/components/header/view-controls.tsx` - BUILT_IN_VIEWS/AVAILABLE_VIEWS deleted; one getViews() loop; transitional RESOURCE_FORK_VIEWS allowlist (Phase 4 deletes); labels pass through t()
- `packages/calendar/src/types/index.ts` - BUILT_IN_VIEWS + BuiltInView deleted (internal-only, sole consumer was view-controls)
- `packages/calendar/src/features/resource-calendar/components/ilamy-resource-calendar/resource-calendar-body.tsx` - Plugin-view fallback via shared ViewRenderer; transitional resourceForkViews map (Phase 4 deletes)
- `packages/calendar/src/features/resource-calendar/components/ilamy-resource-calendar/ilamy-resource-calendar.test.tsx` - +2 failing-first tests (switcher hides resource-incapable plugin views; resource body renders capable ones)
- `packages/calendar/src/features/calendar/components/{day-view → views}` etc. - gutter factory swaps in day-view, week-view (pre-port), resource-day-vertical, resource-month-vertical, resource-week-vertical; HEADER_STAGGER_DELAY swaps in month-header, year-view, week-view, resource-month-horizontal, resource-day-horizontal, resource-week-{horizontal,vertical}-day-header
- `docs/custom-views.md` - NEW: view-author guide (three-knobs table, two-engine ASCII diagrams, engine rule, omission table, worked 40-day example + resource variant)
- `docs/migration-v2.md` - Two new sections (PluginView optional fields non-breaking; resource switcher hides resource-incapable plugin views); stale BUILT_IN_VIEWS sentence fixed
- `docs/hooks-and-context.md`, `docs/types-and-interfaces.md`, `docs/testing-guide.md`, `docs/time-grid.md`, `AGENTS.md` - Path/contract sweep (views/ dir, PluginView section, custom-views links)

## Notes

Phase 3 of the v2 overhaul (`docs/v2-overhaul-phase-3-plan.md`). Re-validation gate findings:
engine navigation logic lives in `features/calendar/hooks/use-calendar-navigation.ts` (Phase 2
slice) — all engine-quoted steps applied there; `use-smart-calendar-context` at its Phase 2
location; `resources`/`orientation` honestly optional on the smart context as the ViewRenderer
requires. Transitional scaffolding that Phase 4 deletes: `RESOURCE_FORK_VIEWS` allowlist in
view-controls.tsx and the `resourceForkViews` map in resource-calendar-body.tsx (both carry
"Phase 4 deletes" comments). All four built-in specs ship `supportsResources: false` until
Phase 4 composes the resource axis; year stays false permanently. Three deliberate behavior
changes, each failing-first: year badges count by range overlap (month-boundary spanners now
appear in both months); resource switcher hides resource-incapable plugin views; resource body
renders resource-capable plugin views instead of a blank body. Unplanned fallout: business-hours.test.tsx
imported DayView/WeekView from the old paths (import-path-only fixes); horizontal-grid-row's
GroupedColumn helper needed `Resource` kept in the @ilamy/types import. Tests: calendar
769 → 774 (+1 year spanning, +2 engine, +2 resource), recurrence stays 154. `bun run ci` green.
