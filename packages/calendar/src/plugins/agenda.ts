// Public subpath entry: `@ilamy/calendar/plugins/agenda`.
//
// The agenda plugin is developed as a separate workspace package
// (@ilamy/calendar-agenda) but is NOT published on its own — it is bundled into
// @ilamy/calendar and re-exported here.
export * from '@ilamy/calendar-agenda'
