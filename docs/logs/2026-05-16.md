# Development Log - 2026-05-16

## Commits (landed)

- `fix(recurrence): treat detached overrides as non-recurring for CRUD` (HEAD on `fix_reccuring_event_edit`)
- `14f7992` — `Fix(style): height of body was not dynamic`
- `75f2304` — `fix(event-form): keep focus on title, description, and location inputs`
- `f5cd499` — `feat(day/week): scroll to current time on view enter`
- `a32e9c3` — `fix: demo page was bursting my GPU`
- `4730366` — `feat(recurrence): presets, full RRULE editor, and aligned demo events`
- `4d62f8c` — `refactor(i18n): format header and view dates with Intl`
- `952b58b` — `chore(i18n): remove unused month and long weekday translation keys`
- `bc61f96` — `refactor(i18n): format weekday labels with Intl and drop sun–sat keys`
- `139a6be` — `feat(i18n): localize DatePicker and Calendar with Intl and locale context`
- `bc8cdd2` — `feat(week-view): align header, all-day row, and time grid columns`
- `0a481d8` — `fix: alignment in allday cell and width of hour column in week-view`
- `60ccebe` — `fix: alignment of gutter columns in week view`
- `300a7d4` — `style: small fixe on border on DayWiew`

## Changes

### Recurrence — detached override fix (HEAD)

- **`isRecurringEvent`**: return `false` when `recurrenceId` is set and `rrule` is absent (detached scope-`"this"` overrides).
- **Why**: after editing one occurrence, drag/form edit must call `updateEvent` / `deleteEvent`, not reopen the recurrence dialog — a second `updateRecurringEvent` duplicated events.
- **Tests**: `crud-operations.test.ts`, `calendar-dnd-context.test.tsx`.
- **Demo**: `handleEventAdd` / `handleEventUpdate` use `console.log` instead of `alert` (easier to inspect synthetic instance ids).

### Layout & demo (`14f7992`, `a32e9c3`)

- **`14f7992`**: dynamic body height so the calendar fills the viewport.
- **`a32e9c3`**: demo page GPU load fix.

### Event form & day/week views (`75f2304`, `f5cd499`)

- **`75f2304`**: preserve focus on title, description, and location while editing.
- **`f5cd499`**: scroll to current time when entering day/week view.

### Recurrence — editor & yearly layout (`4730366` and earlier)

- Preset select (Once, daily, weekly on …, customize), full customize RRULE UI, `referenceDate`.
- Intl for weekday/month labels (`formatLocaleWeekday`, `formatLocaleMonth`, `getOnTheDaySelectionLabel`).
- `recurrence-preset-utils.ts`, `rrule-editor-utils.ts` + tests; `weekly-recurrence-fields`, `monthly-recurrence-fields`.
- Yearly customize: month in frequency row (`RecurrenceMonthSelect`, `yearly-on-month-select`); on day / on the via `MonthlyRecurrenceFields` (`testIdPrefix="yearly"`).
- **Translations**: recurrence preset/UI keys (`recurrenceOnce`, `onDay`, `first`, …); **93** keys (calendar date names via `Intl`).
- **event-form**, **radio-group**, **seed/demo**: wired to new recurrence editor.

### i18n

- Header, views, DatePicker use `Intl` + `locale`; `pickADate`, `previousMonth`, `nextMonth` keys.
- Removed month + weekday name translation keys (38).

### Week view layout

- Column alignment for header, all-day row, time grid; gutter and hour-column fixes.

## Files Modified (HEAD — detached override fix)

- `src/features/recurrence/utils/recurrence-handler.ts` — `isRecurringEvent` excludes detached overrides
- `src/features/recurrence/utils/crud-operations.test.ts` — assertions + scope-`"this"` regression
- `src/components/drag-and-drop/calendar-dnd-context.test.tsx` — detached instance expects `false`
- `src/components/demo/demo-page.tsx` — `console.log` for add/update handlers
- `docs/logs/2026-05-16.md`

## Files Modified (`4730366` and earlier)

- `src/features/recurrence/components/recurrence-editor/*`, `src/features/recurrence/utils/*`
- `src/components/event-form/event-form.tsx`, `event-form.test.tsx`
- `src/components/ui/radio-group.tsx`, `date-picker.tsx`, `calendar.tsx`
- `src/lib/utils/date-locale-format.ts`, `src/lib/translations/*`
- `src/features/calendar/components/week-view/*`, header/views i18n files

## Notes

- `RecurrenceEditor` requires `CalendarProvider`; preset labels use `recurrence*` keys (not `sun`/`january`).
- **Synthetic instance ids**: `generateRecurringEvents` assigns `${baseId}_${index}` (e.g. `meeting_0`) for UI occurrences; stored `currentEvents` keeps the base id. `onEventUpdate` from `updateRecurringEvent` currently spreads the **instance** passed in (with `_0`), while persistence resolves the series via `uid` in `findBaseEventIndex`.
- Recurrence-editor tests: **74** pass; full suite **880** pass (current).
