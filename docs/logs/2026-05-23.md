# Development Log - 2026-05-23

## Changes

- **[week-view / regression test]**: Added and stabilized a regression test in `week-view.test.tsx` to prevent silent alignment drift between the fixed time gutter and week day/all-day columns during future `CELL_CLASS` refactors.
- **[week-view / spacing scenario]**: Verified the alignment contract under a non-default `--spacing` CSS variable by rendering WeekView in a styled wrapper in tests.
- **[tests / compatibility fix]**: Replaced a branch-specific class assertion (legacy `calc(...)` width classes) with a stable contract assertion that matches current implementation (`flex-1` fluid widths on day/all-day columns + fixed gutter width classes).
- **[validation]**: Ran focused and full week-view test runs to confirm the regression test is green in this branch.

## Files Modified

- `src/features/calendar/components/week-view/week-view.test.tsx` - Added custom-`--spacing` alignment regression test and adjusted assertions to stable layout guarantees.
- `docs/logs/2026-05-22.md` - Appended follow-up notes during late-night fixes before date rollover.
- `docs/logs/2026-05-23.md` - Added today’s required development log entry.

## Notes

The test now guards behavior (alignment contract) rather than a single historical class-string implementation, which reduces false failures across branches while still catching real regressions.
