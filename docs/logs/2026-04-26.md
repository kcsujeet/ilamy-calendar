# Development Log - 2026-04-26

## Changes

- **triage**: Reviewed PR #98 (`feature/hide-hours-in-week-day-view` by mark-tutton) to plan takeover.
- **merge**: Merged `main` into `feature/hide-hours-in-week-day-view`. Three conflicts resolved (uncommitted, awaiting verification + user approval before commit).

## Files Modified

- `src/components/vertical-grid/vertical-grid-header-container.tsx` — conflict: kept Mark's `expandAllDayRow` flow; dropped `border-b` from the all-day container since main's commit `1d17ec9` moved that border into the inner all-day cells.
- `src/features/calendar/hooks/useProcessedDayEvents.ts` — conflict: kept main's structure (`first`/`last` intermediate vars, `Dayjs` type alias, `filterEventsByResource` util). Mark's empty-range guard already exists on main, so no behavior lost.
- `src/features/calendar/hooks/useProcessedWeekEvents.ts` — same resolution shape as `useProcessedDayEvents.ts`.

## Notes

PR #98 fixes a week-view crash when `hideNonBusinessHours` produces an empty hour range, and adds an expanded all-day layout when no hourly rows are visible.

State at session start:
- Review status: changes requested. One inline comment on `src/features/calendar/components/week-view/week-view.tsx:91` — "we need to do this for vertical resource views too."
- Merge state: DIRTY (conflicts with main; ~20 commits on main since the PR forked).
- CI on the PR last passed at commit `d9f58dc`, but author noted unrelated Biome import-order failures in `src/components/ui/{card,popover,tabs}.tsx` — those `ui/` files are now main's versions, so the issue should be gone.

Outstanding decisions before takeover work continues:
1. Push to Mark's fork branch vs. a new branch on `kcsujeet/ilamy-calendar`. (Pending.)

Remaining takeover plan:
1. Verify merge with `bun run type-check` + `bun test`.
2. Apply the empty-hours / expanded all-day handling to the vertical resource calendar views (review feedback).
3. Add resource-view regression tests.
4. Run `bun run ci`.
5. Update this dev log with the resource-view changes.
