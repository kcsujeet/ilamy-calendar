# Development Log - 2026-06-01

## Changes

- **plugins**: Added `contribute` plugin hook and `collect` runtime method (Task A2 of v2 plugin-architecture refactor). Plugins can now contribute arbitrary data to a named string "point"; the host or other plugins gather all contributions via `collect`. Additive semantics — all plugins may contribute; results are aggregated by flatMap.

## Files Modified

- `src/features/plugins/lib/types.ts` — Added `contribute?: (point: string, context: unknown) => unknown[]` to `IlamyPlugin`; added `collect: (point: string, context: unknown) => unknown[]` to `PluginRuntime`.
- `src/features/plugins/lib/create-plugin-runtime.ts` — Implemented `collect` via `plugins.flatMap((plugin) => plugin.contribute?.(point, context) ?? [])`.
- `src/features/plugins/lib/create-plugin-runtime.test.ts` — Added 3 new tests (written first, confirmed failing before implementation).
- `src/hooks/use-calendar-engine.ts` — Added `collect` to `CalendarEngineReturn` interface and returned object.
- `src/features/calendar/contexts/calendar-context/context.ts` — Added `collect` to `CalendarContextType`.

## Notes

TDD followed strictly: tests written first, confirmed 3 failures, then implemented. Full suite: 900 pass / 0 fail. Type-check clean. Lint clean.

---

## Changes (Task A3a)

- **plugins**: Task A3a: added PluginView + views/provider on IlamyPlugin and getViews/getProviders on the runtime (contract + runtime only, no calendar wiring yet).

## Files Modified (Task A3a)

- `src/features/plugins/lib/types.ts` — Added `PluginView` interface; added `views?: PluginView[]` and `provider?: ComponentType<{ children: ReactNode }>` to `IlamyPlugin`; added `getViews` and `getProviders` to `PluginRuntime`. Added `ComponentType` and `ManipulateType` imports.
- `src/features/plugins/lib/create-plugin-runtime.ts` — Implemented `getViews` (flatMap over plugin.views) and `getProviders` (map + type-guard filter over plugin.provider). Added `ComponentType` to react import.
- `src/features/plugins/lib/create-plugin-runtime.test.ts` — Added 3 new tests (written first, confirmed failing before implementation). Added `ReactNode` to react import.

## Notes (Task A3a)

TDD followed strictly: 3 tests written first, confirmed failing (TypeError: getViews/getProviders not a function), then implemented. Full suite: 903 pass / 0 fail. Type-check clean. Lint clean.

---

## Changes (Task A3b)

- **calendar**: Task A3b: CalendarView widened to string (+ BUILT_IN_VIEWS); engine surfaces getViews/getProviders and resolves navigation unit for plugin views; CalendarContent renders plugin views; ViewControls lists plugin views. Built-in behavior unchanged.

## Files Modified (Task A3b)

- `src/types/index.ts` — Changed `CalendarView` to `string`; added `BUILT_IN_VIEWS` const and `BuiltInView` type.
- `src/hooks/use-calendar-engine.ts` — Added `getViews`/`getProviders` to `CalendarEngineReturn` and returned object; `VIEW_UNITS` typed `Record<string, ManipulateType>`; `navigatePeriod` resolves unit via `VIEW_UNITS[view] ?? plugin navigationUnit ?? 'day'` (added `pluginRuntime` to deps); added `ComponentType` + `PluginView` imports; comment on default range branch.
- `src/features/calendar/components/ilamy-calendar.tsx` — `CalendarContent` pulls `getViews` via selector; renders built-in or plugin view component.
- `src/components/header/view-controls.tsx` — Widened types to `CalendarView`/`BuiltInView`; pulled `getViews`; appended plugin-view buttons after built-ins.
- `src/features/calendar/contexts/calendar-context/context.ts` — Added `getViews: () => PluginView[]` to `CalendarContextType` (+ `PluginView` import).

## Notes (Task A3b)

Integration wiring only; no new tests / no provider composition (Task A3c). Full suite: 903 pass / 0 fail. Type-check clean. Lint clean.

---

## Changes (Task A3c)

- **plugins**: Task A3c: plugin providers compose around the calendar subtree (both CalendarProvider and ResourceCalendarProvider); added an e2e test proving a plugin view renders and reads its plugin provider's context.

## Files Modified (Task A3c)

- `src/features/calendar/contexts/calendar-context/provider.tsx` — Calls `calendarEngine.getProviders()` and wraps `children` via `reduceRight` before passing to `CalendarContext.Provider`.
- `src/features/resource-calendar/contexts/resource-calendar-context/provider.tsx` — Same change for `ResourceCalendarContext.Provider`.
- `src/features/calendar/components/ilamy-calendar.test.tsx` — Added top-level `test` for plugin view + provider e2e; added `createContext`, `useContext`, `IlamyPlugin`, and `test` imports.

## Notes (Task A3c)

TDD followed: test written first, then providers updated. Added `key={index}` to the JSX inside `reduceRight` to satisfy the `useJsxKeyInIterable` lint rule. Full suite: 904 pass / 0 fail. Type-check clean. Lint clean.

---

## Changes (Task B1+B2)

- **engine/context**: Task B1: added generic scoped-mutation methods `applyScopedEdit(event, updates, scope)` and `applyScopedDelete(event, scope)` to the calendar engine and `CalendarContextType`. They mirror the recurrence-named `updateRecurringEvent`/`deleteRecurringEvent` bodies but take an opaque `scope` and resolve the managing plugin via `pluginRuntime.getEventManager(event)`. The recurrence-named methods are intentionally kept (removed in Task B3).
- **hooks**: Task B2: added shared `useScopedEventMutation` hook (generalized from `useRecurringEventActions`) in `src/hooks`. Manages the scope dialog state (`open(Edit|Delete)Dialog`, `closeDialog`, `handleConfirm`) and applies via the new generic context methods.
- **host migration**: `event-form.tsx` and `calendar-dnd-context.tsx` now use `useScopedEventMutation` instead of the recurrence-specific hook / inline `recurringDialog` state. `useRecurringEventActions` is now unused (kept for Task B3).

## Files Modified (Task B1+B2)

- `src/hooks/use-calendar-engine.ts` — Added `applyScopedEdit`/`applyScopedDelete` to `CalendarEngineReturn`, implemented both via `useCallback` (deps mirror the recurring methods), returned them.
- `src/features/calendar/contexts/calendar-context/context.ts` — Added `applyScopedEdit`/`applyScopedDelete` to `CalendarContextType`.
- `src/hooks/use-scoped-event-mutation.ts` — New shared host hook.
- `src/hooks/use-scoped-event-mutation.test.ts` — New: 4 focused tests (starts closed; edit confirm applies + closes; delete confirm applies + closes; closeDialog dismisses without applying), rendered inside a real `CalendarProvider` with a fake plugin returning sentinel results.
- `src/components/event-form/event-form.tsx` — Swapped import + hook call to `useScopedEventMutation(onClose)`; `operationType` → `operation`; `resolve` passes `scope` through (no `as never`).
- `src/components/drag-and-drop/calendar-dnd-context.tsx` — Removed inline `recurringDialog` state, `handleRecurringEventConfirm`, `handleRecurringEventClose`, `CLOSED_DIALOG`; uses `useScopedEventMutation()`; `performEventUpdate` calls `openEditDialog`; renders scope dialog from `dialogState`; dropped `updateRecurringEvent` from the context selector.

## Notes (Task B1+B2)

TDD: hook test written before the hook implementation. Full suite: 908 pass / 0 fail (was 904, +4 new). Type-check clean. Lint clean (biome). `useRecurringEventActions` and the recurrence-named context methods deliberately NOT removed (Task B3).

---

## Changes (Task B3)

- **event-form**: Made `event-form.tsx` fully recurrence-agnostic. Removed the `RRuleOptions` import, `rrule` state, `handleRRuleChange`, and the `parentEvent`/`findParentRecurringEvent` usage. Introduced a generic `pluginUpdates` draft (`useState<Partial<CalendarEvent>>({})`); the `SLOT_EVENT_FORM` `onChange` merges arbitrary plugin fields into it, and the slot `event` is `{ ...selectedEvent, ...pluginUpdates }`. Both the saved `eventData` and the scoped edit-dialog `updates` now spread `...pluginUpdates` instead of `rrule: rrule || undefined`.
- **recurrence plugin**: Parent-series lookup moved into a new `RecurrenceFormSection` component (renders `RecurrenceEditor`, reads context, finds the base rrule event by uid). The recurrence plugin's `SLOT_EVENT_FORM` branch now renders `<RecurrenceFormSection event onChange />`. The section anchors a new rule's `dtstart` to the event's start (previously done in the form), keeping recurrence knowledge inside the plugin.
- **context/engine cleanup**: Removed the recurrence-named context API — `updateRecurringEvent`, `deleteRecurringEvent`, `findParentRecurringEvent` (and the `RecurrenceEditOptions` import) from `use-calendar-engine.ts` and `CalendarContextType`. Scoped recurring edit/delete now flows solely through `applyScopedEdit`/`applyScopedDelete`.
- **hook deletion**: Deleted the orphaned `useRecurringEventActions.ts` (no remaining references; superseded by `useScopedEventMutation` in B2).

## Files Modified (Task B3)

- `src/components/event-form/event-form.tsx` — Generic `pluginUpdates` draft; removed all recurrence-specific state/imports.
- `src/features/plugins/recurrence/components/recurrence-form-section.tsx` — New component owning the parent-series lookup + dtstart anchoring.
- `src/features/plugins/recurrence/recurrence-plugin.tsx` — `SLOT_EVENT_FORM` renders `RecurrenceFormSection`.
- `src/hooks/use-calendar-engine.ts` — Removed `updateRecurringEvent`/`deleteRecurringEvent`/`findParentRecurringEvent` + `RecurrenceEditOptions` import.
- `src/features/calendar/contexts/calendar-context/context.ts` — Removed the same three methods + `RecurrenceEditOptions` import.
- `src/hooks/use-smart-calendar-context.ts` — Exposed `rawEvents` on `useIlamyCalendarContext` so the recurrence section can find the (unexpanded) base series; generated instances drop their rrule, so the base only exists in raw events.
- `src/features/plugins/recurrence/hooks/useRecurringEventActions.ts` — Deleted (and now-empty `hooks/` dir removed).
- `src/hooks/use-calendar-engine.test.ts` — Migrated recurring update/delete tests to `applyScopedEdit`/`applyScopedDelete`; removed the two `findParentRecurringEvent` engine tests (lookup moved to the plugin).
- `src/features/calendar/contexts/calendar-context/provider.test.tsx` — Dropped `findParentRecurringEvent` from the shared `TestComponent` and removed the dedicated lookup describe block; migrated recurring callback tests to `applyScopedEdit`/`applyScopedDelete`.

## Notes (Task B3)

`useIlamyCalendarContext().events` is the plugin-expanded list (base rrule events are replaced by generated instances with `rrule: undefined`), so the parent-series lookup uses `rawEvents` instead — exposed publicly for this. event-form imports nothing from the recurrence plugin. Full suite: 903 pass / 0 fail. Type-check clean. Lint clean.

---

## Changes (Task C1)

- **types/recurrence plugin**: Made core `CalendarEvent` lean — removed the recurrence-specific fields `rrule`, `recurrenceId`, `exdates` (and the `RRuleOptions` import) from `src/components/types.ts`. The recurrence plugin re-adds these fields globally via TypeScript declaration merging in a new `augment.ts`, so all existing reads (recurrence plugin, `seed.ts`, `export-ical.ts`) keep type-checking without re-coupling the core type to recurrence.

## Files Modified (Task C1)

- `src/components/types.ts` — Removed `rrule`/`recurrenceId`/`exdates` from `CalendarEvent` and the `RRuleOptions` import. All other fields unchanged.
- `src/features/plugins/recurrence/augment.ts` — New. `declare module '@/components/types'` re-adds `rrule?: RRuleOptions`, `recurrenceId?: string`, `exdates?: string[]` to `CalendarEvent`.
- `src/features/plugins/recurrence/recurrence-plugin.tsx` — Added side-effect import `import './augment'` at the top so importing the plugin pulls in the augmentation.

## Notes (Task C1)

Augmentation specifier `@/components/types` exactly matches how `CalendarEvent` is imported everywhere, so the merge applies globally within the tsc program (augment.ts is covered by the include glob). `CalendarEvent` was already an `interface`, so no conversion was needed. Full suite: 903 pass / 0 fail. Type-check clean. Lint clean.

---

## Changes (Task D1)

- **core iCal exporter**: Made `src/lib/utils/export-ical.ts` recurrence-agnostic. It now serializes only CORE VEVENT fields (UID, DTSTART/DTEND, SUMMARY, DESCRIPTION, LOCATION, DTSTAMP/CREATED/LAST-MODIFIED, STATUS/SEQUENCE/TRANSP) and appends plugin-contributed lines via `collect('ical:vevent-properties', event)` (string-typed only) before `END:VEVENT`. Deleted `filterEvents` — the exporter runs on raw events (no generated instances) so no dedupe/filter is needed; it iterates all events. Removed all reads of `event.rrule` / `event.recurrenceId` / `event.exdates` and the `formatRRule` helper.
- **exporter signature**: `collect` inserted as the 2nd parameter of `exportToICalendar(events, collect, calendarName?)` and `downloadICalendar(events, collect, filename?, calendarName?)`.
- **recurrence plugin contributes its iCal lines**: New `src/features/plugins/recurrence/ical.ts` exports `recurrenceICalProperties(event)` returning the `RRULE` / `EXDATE` / `RECURRENCE-ID` lines (formatting moved out of the core exporter; uses dayjs from `@/lib/configs/dayjs-config`). The recurrence plugin now implements `contribute(point, context)`: returns `recurrenceICalProperties(context)` for `point === 'ical:vevent-properties'`, else `[]`.
- **caller**: `base-header.tsx` pulls `collect` from context and passes it: `downloadICalendar(rawEvents, collect, filename, 'ilamy Calendar')`.

## Files Modified (Task D1)

- `src/lib/utils/export-ical.ts` — Recurrence-free core exporter; `collect` param; `filterEvents`/`formatRRule` deleted.
- `src/features/plugins/recurrence/ical.ts` — New. `recurrenceICalProperties(event)`.
- `src/features/plugins/recurrence/recurrence-plugin.tsx` — Added `contribute` hook + imports.
- `src/components/header/base-header.tsx` — Reads `ctx.collect`, passes it to `downloadICalendar`.
- `src/lib/utils/export-ical.test.ts` — Updated all call sites for new signature; core-only tests use a `noCollect` no-op, recurrence tests use a `recurrenceCollect` backed by `recurrenceICalProperties`; replaced the two `filterEvents` tests with no-filter serialization assertions (assertions otherwise unchanged).
- `src/features/plugins/recurrence/recurrence-plugin.test.ts` — Added `contribute` cases: RRULE for a base event, RRULE+EXDATE for an event with exdates, RECURRENCE-ID for a modified instance, `[]` for an unrelated point.
- `src/components/header/base-header.test.tsx` — Updated `downloadICalendar` mock expectation to include `expect.any(Function)` (collect) in the new arg order.

## Notes (Task D1)

`collect` was already on the calendar context (`pluginRuntime.collect`), so the header wiring was a one-line addition. The `'ical:vevent-properties'` point string is shared informally between the recurrence plugin's `contribute` and the core exporter's `collect` call — no shared constant introduced (kept the core agnostic of the point name's meaning). Grep confirms `export-ical.ts` has no `rrule`/`recurrenceId`/`exdates`/`filterEvents`. Full suite: 907 pass / 0 fail. Type-check clean. Lint clean. Biome format clean.

---

## Changes (Task E1)

- **recurrence plugin UI independence**: The recurrence plugin now ships its OWN minimal UI under `src/features/plugins/recurrence/ui/` and no longer imports the host design system (`@/components/ui/*`). This proves a third-party plugin can build its UI without our internal components. The new primitives are self-contained React + Tailwind (no Radix / no new dependency), favoring native form controls for simplicity and accessibility.
- **editor/dialog rewired**: `recurrence-editor.tsx` and `recurrence-edit-dialog.tsx` now import from `../../ui/*`. The frequency dropdown switched from the shadcn `Select`/`SelectTrigger`/`SelectContent`/`SelectItem` composition to a native `<select>` with `<option>` children (same selectable values/labels, same emitted rrule). All other behavior (toggle, interval, weekday selection, end-type/count/until, scope confirmation) is unchanged.

## Files Modified (Task E1)

- `src/features/plugins/recurrence/ui/button.tsx` — New. Styled native `<button>` with `default`/`outline` variants. `Button`.
- `src/features/plugins/recurrence/ui/input.tsx` — New. Styled native `<input>`. `Input`.
- `src/features/plugins/recurrence/ui/label.tsx` — New. Styled native `<label>`. `Label`.
- `src/features/plugins/recurrence/ui/checkbox.tsx` — New. Native `<input type="checkbox">` exposing `checked` + `onCheckedChange`. `Checkbox`.
- `src/features/plugins/recurrence/ui/select.tsx` — New. Native `<select>` taking `value` + `onValueChange` and `<option>` children. `Select`.
- `src/features/plugins/recurrence/ui/date-picker.tsx` — New. Native `<input type="date">` taking `date: Date | undefined` + `onChange`; converts to/from `YYYY-MM-DD` via dayjs. `DatePicker`.
- `src/features/plugins/recurrence/ui/card.tsx` — New. `Card`/`CardHeader`/`CardTitle`/`CardContent` styled divs/heading.
- `src/features/plugins/recurrence/ui/dialog.tsx` — New. Minimal accessible modal: fixed overlay + centered panel, `role="dialog"`, `aria-modal="true"`, closes on Escape and overlay click via `onOpenChange`. Exposes `Dialog`/`DialogContent` (with a close X button)/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter`.
- `src/features/plugins/recurrence/components/recurrence-editor/recurrence-editor.tsx` — Swapped all `@/components/ui/*` imports for `../../ui/*`; native-select frequency control.
- `src/features/plugins/recurrence/components/recurrence-edit-dialog/recurrence-edit-dialog.tsx` — Swapped `@/components/ui/{button,dialog}` for `../../ui/{button,dialog}`; passes `onClose` to `DialogContent` for the close button.
- `src/features/plugins/recurrence/components/recurrence-editor/recurrence-editor.test.tsx` — Frequency-change tests now drive the native `<select>` via `fireEvent.change` (new local `selectFrequency` helper) instead of clicking a shadcn combobox popup + option. Same behavioral assertions.

## Notes (Task E1)

`grep -rn "@/components/ui" src/features/plugins/recurrence` is now EMPTY. `cn` from `@/lib/utils` is still used (it is not part of the host design system). Behavior approximation: the styled shadcn dropdown is now a native `<select>` — same options, values, and emitted rrule; the editor tests assert the resulting `onChange`/rrule, which are unchanged. The edit-dialog test still finds 5 buttons (3 scopes + cancel + close) and confirms `onConfirm(scope)`/`onClose`. Recurrence tests: 141 pass / 0 fail. Full suite: 907 pass / 0 fail. Type-check clean. Lint clean (a11y items on the new overlay/label are biome warnings, not errors). Biome format clean.

---

## Changes (Task F1)

- **engine (BREAKING)**: Plugins are now opt-in. The calendar core ships ZERO plugins by default — `useCalendarEngine` now defaults `plugins` to `[]` (was `[recurrencePlugin()]`) and no longer imports `recurrencePlugin`. This makes recurrence tree-shakable and the core plugin-free. **BREAKING: recurrence no longer works out of the box; consumers must pass `plugins={[recurrencePlugin()]}`.**
- **demo opt-in**: The demo registers the recurrence plugin (module-level `demoPlugins = [recurrencePlugin()]`, stable reference) on both `IlamyCalendar` and `IlamyResourceCalendar`, since `seed.ts` has recurring events that need expansion.
- **examples**: None of `examples/{vite,nextjs,astro}` use recurring events (no `rrule`/`RRule`/`recurrenceId` in their source — only transitive lockfile refs), so they were left untouched.
- **tests**: Re-registered the recurrence plugin in the recurrence-dependent tests that relied on the old default. No assertions were weakened.

## Files Modified (Task F1)

- `src/hooks/use-calendar-engine.ts` — `plugins` default `[recurrencePlugin()]` → `[]`; removed the `recurrencePlugin` import.
- `src/features/calendar/types/index.ts` — Updated the `plugins` prop doc comment (no longer defaults to the built-in recurrence plugin).
- `src/components/demo/demo-page.tsx` — Added `recurrencePlugin` import + module-level `demoPlugins`; pass `plugins={demoPlugins}` to both calendar components.
- `src/hooks/use-calendar-engine.test.ts` — Added `recurrencePlugin` import; registered `plugins: [recurrencePlugin()]` in the 4 recurrence tests (getEventsForDateRange generation; scoped update-all; scoped delete-all; scoped delete-all without uid).
- `src/features/calendar/contexts/calendar-context/provider.test.tsx` — Added `recurrencePlugin` import; registered the plugin in the 6 recurrence renderProvider calls (On-Demand generation + EXDATE exclusions; no-duplicates; recurring callbacks; rrule update; time/date update).
- `src/components/event-form/event-form.test.tsx` — Added `recurrencePlugin` import; registered the plugin via `providerProps` in the 4 recurrence tests (render editor; toggle recurrence; recurrence in submission; pull RRULE from parent instance).

## Notes (Task F1)

`grep -n "recurrence" src/hooks/use-calendar-engine.ts` is now EMPTY (engine is recurrence-free). The recurrence plugin's own unit tests call `recurrencePlugin()` directly and were untouched. Non-recurrence tests were left as-is. Full suite: 907 pass / 0 fail. Type-check clean. Lint clean (biome). Biome check (incl. import organization) clean on all touched files.

---

## Changes (Task F2)

- **public API surface**: Renamed `UseIlamyCalendarContextReturn` → `IlamyCalendarApi` (all references updated). Added the v2 plugin-system members to the interface and to the `useIlamyCalendarContext()` assembly: `t`, `timeFormat`, `timezone`, `currentLocale`, `getEventsForDateRange`, `applyScopedEdit`, `applyScopedDelete`, `getEventManager`, `renderSlot`, `collect`, `getViews`. Added the doc comment declaring it "the public calendar API surface". The full internal context stays behind the unexported `useSmartCalendarContext`.

## Files Modified (Task F2)

- `src/hooks/use-smart-calendar-context.ts` — Renamed interface; added plugin/util members to type and assembly function; added `React`, `IlamyPlugin`, `PluginView`, `TranslatorFunction`, `TimeFormat` imports.
- `src/index.ts` — Replaced `UseIlamyCalendarContextReturn` export with `IlamyCalendarApi`.

## Notes (Task F2)

All new members were already present on `CalendarContextType` (via `context.ts`) and thus on `SmartCalendarContextType` — no missing fields. `currentLocale` is `string | undefined` on the context, so the assembly uses `?? ''` to satisfy the `readonly currentLocale: string` signature. Grep `UseIlamyCalendarContextReturn src/` → empty. Grep `useSmartCalendarContext src/index.ts` → empty. Full suite: 907 pass / 0 fail. Type-check clean. Lint clean.

---

## Changes (Task F3a)

- **public plugin SDK / self-reference**: Established `@ilamy/calendar` as the public plugin SDK and made the recurrence plugin dogfood it. Added a `@ilamy/calendar` self-reference tsconfig path so internal code and tsc resolve the package name to our own source.
- **main entry**: `src/index.ts` now exports the full plugin SDK and a public configured dayjs, and NO LONGER re-exports recurrence (tree-shaking prep). Added: `IlamyPlugin`, `PluginDateRange`, `PluginMutationArgs`, `PluginView` (plugin contract types); `SLOT_EVENT_FORM`, `SLOT_EVENT_MUTATION_SCOPE`, `EventFormSlotContext`, `EventMutationScopeSlotContext` (slot catalog); `dayjs` (configured instance) + `Dayjs`/`ManipulateType` types. Removed: `generateRecurringEvents`, `isRecurringEvent`, `RRule`, `Weekday`, `RRuleOptions`. `createPluginRuntime` is NOT exported (internal).
- **recurrence subpath entry**: New `src/features/plugins/recurrence/recurrence.ts` — the published surface for `@ilamy/calendar/plugins/recurrence`. Side-effect `import './augment'`; re-exports `recurrencePlugin`, `generateRecurringEvents`, `isRecurringEvent`, `RRule`, `Weekday`, and the `RRuleOptions` type.
- **augment targets public name**: `augment.ts` changed from `declare module '@/components/types'` to `declare module '@ilamy/calendar'` (with `import '@ilamy/calendar'`). Type-check is CLEAN with the `@ilamy/calendar`-ONLY form — merging through the package entry's re-export reaches all internal readers of `event.rrule`. No internal `@/components/types` augmentation was needed.
- **recurrence source dogfoods the public API**: All non-test recurrence source now imports ONLY from `@ilamy/calendar` (+ local utilities). `CalendarEvent`, `useIlamyCalendarContext`, `dayjs`/`Dayjs`/`ManipulateType`, slot constants/types, and plugin contract types all come from `@ilamy/calendar`. Self-referential `@/features/plugins/recurrence/...` imports were converted to relative paths.

## Files Modified (Task F3a)

- `tsconfig.json` — Added `@ilamy/calendar` → `./src/index.ts` and `@ilamy/calendar/plugins/recurrence` → recurrence subpath entry to `compilerOptions.paths` (kept `@/*`).
- `src/index.ts` — Plugin SDK + dayjs exports added; recurrence re-exports removed.
- `src/features/plugins/recurrence/recurrence.ts` — New subpath entry.
- `src/features/plugins/recurrence/augment.ts` — Now `declare module '@ilamy/calendar'`.
- `src/features/plugins/recurrence/ui/cn.ts` — New local `cn` (clsx + tailwind-merge), since `cn` is not public.
- `src/features/plugins/recurrence/utils/helpers.ts` — New local `omitKeys`/`safeDate` (not part of the public surface).
- `src/features/plugins/recurrence/ui/{button,card,checkbox,dialog,input,label,select,date-picker}.tsx` — `cn` now from `./cn`; date-picker dayjs from `@ilamy/calendar`.
- `src/features/plugins/recurrence/utils/recurrence-handler.ts` — Imports `CalendarEvent`/`dayjs`/`Dayjs` from `@ilamy/calendar`, `RRuleOptions` from `../types`, `omitKeys`/`safeDate` from `./helpers`.
- `src/features/plugins/recurrence/ical.ts` — `CalendarEvent`/`dayjs`/`Dayjs` from `@ilamy/calendar`.
- `src/features/plugins/recurrence/recurrence-plugin.tsx` — Slot constants/types, plugin types, `CalendarEvent` from `@ilamy/calendar`.
- `src/features/plugins/recurrence/components/recurrence-form-section.tsx` — `CalendarEvent` + `useIlamyCalendarContext` from `@ilamy/calendar`.
- `src/features/plugins/recurrence/components/recurrence-edit-dialog/recurrence-edit-dialog.tsx` — `useIlamyCalendarContext` from `@ilamy/calendar` (replaced `useSmartCalendarContext` selector); types/ui via relative paths.
- `src/features/plugins/recurrence/components/recurrence-editor/recurrence-editor.tsx` — `dayjs` + `useIlamyCalendarContext` from `@ilamy/calendar` (replaced `useSmartCalendarContext` selector); types/ui via relative paths; inlined a local `listKey` helper (replacing `keys.listKey` from `@/lib/utils/keys`).
- `src/features/plugins/recurrence/types/index.ts` — `Dayjs` from `@ilamy/calendar`.

## Notes (Task F3a)

Augment form that compiled: `declare module '@ilamy/calendar'` ONLY (no internal `@/components/types` fallback needed) — type-check clean. Caveats / things to flag: (1) `omitKeys`/`safeDate` and `cn` are NOT part of the public `@ilamy/calendar` surface, so the recurrence plugin now ships local copies under `utils/helpers.ts` and `ui/cn.ts` (plugin brings its own utilities). (2) `keys.listKey` (from `@/lib/utils/keys`) was a trivial `parts.join('-')` and was inlined locally in the editor. (3) The two `useSmartCalendarContext((ctx) => …)` selector calls were converted to the public `useIlamyCalendarContext()` (full API object) since `t` and `firstDayOfWeek` are on `IlamyCalendarApi`. (4) `types/index.ts` wasn't in the task's explicit Step 5 file list but is non-test recurrence source with a dayjs import, so it was switched too (needed for the grep to be clean). (5) Running `bun run check:fix` reformatted import ordering across several pre-existing-but-unstaged files from earlier F-tasks on this branch (formatting only, no behavior change); 4 unsafe suggestions in `@/lib/utils/index.ts` were left unapplied. Grep: `src/index.ts` recurrence-free (empty); recurrence non-test source `@/` imports (empty). Did NOT touch package.json `exports` or bunup config (Task F3b). Full suite: 907 pass / 0 fail. Type-check clean. Lint clean (biome).

---

## Changes (Task F3b)

- **bunup multi-entry**: Added the recurrence subpath as a second `entry` in the single `defineConfig({...})` (entry as `Arrayable<string>`: `["src/index.ts", "src/features/plugins/recurrence/recurrence.ts"]`). bunup maps each entry to an output path mirroring its location under the entries lowest-common-ancestor (`src/`); with ESM code-splitting (default) the shared core lands in `dist/shared/chunk-*.js`, `dist/index.js` is a re-export shim, and the recurrence-only code is `dist/features/plugins/recurrence/recurrence.{js,d.ts}` (88KB, sharing the common chunk rather than duplicating it). bunup has NO per-entry output-name/record map and no glob rename, so `dist/plugins/recurrence.js` is not directly achievable; `exports` points at the real emitted path instead (Step 4 deviation, documented).
- **package.json exports**: `exports` now maps `.` -> core (`types` then `import`) and `./plugins/recurrence` -> `./dist/features/plugins/recurrence/recurrence.{d.ts,js}`. `main`/`module`/`types` still point at the core build for back-compat. `files` already includes `dist`.
- **sideEffects**: NOT added to source package.json (verified it still breaks the build: bun tree-shakes everything to a 461-byte empty `index.js`). `sideEffects: false` is injected only at publish time by the existing `scripts/prepack.ts`; verified prepack emits `sideEffects: false` alongside both export entries, and postpack restores. Consumers therefore get a tree-shakeable package.
- **augmentation preserved in d.ts**: The dts bundler tree-shook the side-effect-only `import "./augment"`, dropping the `declare module "@ilamy/calendar"` block (rrule/recurrenceId/exdates) from `recurrence.d.ts`. Fixed by adding a type-only marker export `RecurrenceAugmentation` in `augment.ts` and re-exporting it (`export type`) from `recurrence.ts`. No runtime logic change. The augmentation now appears in `recurrence.d.ts`.

## Files Modified (Task F3b)

- `bunup.config.ts` - Added recurrence entry to the `entry` array; documented the LCA output-path mapping.
- `package.json` - `exports` map adds `./plugins/recurrence`; core entry reordered to `types`-then-`import`.
- `src/features/plugins/recurrence/augment.ts` - Added type-only `RecurrenceAugmentation` marker export so the dts bundler keeps the augmentation module.
- `src/features/plugins/recurrence/recurrence.ts` - Re-exports the marker via `export type { RecurrenceAugmentation }` (replaced the bare side-effect import) to keep the augmentation in the emitted d.ts.

## Notes (Task F3b)

bunup multi-entry syntax confirmed from installed `node_modules/bunup` types (`BuildOptions.entry: Arrayable<string>`, `defineConfig(item | namedItem[])`, `sourceBase` controls output structure via LCA). Verification greps: core `dist/index.js` + `dist/shared/chunk-*.js` `generateRecurringEvents|RRule` = 0 and `rrule` = 0 (recurrence absent from core); `dist/index.d.ts` `rrule` = 0 (lean core CalendarEvent); `dist/features/plugins/recurrence/recurrence.d.ts` `rrule` = 6 incl. the `declare module` augmentation. Core bundle (shared chunk) = 535.29KB raw / 148.46KB gzip (was ~535KB standalone; the historical ~615KB included recurrence). type-check clean, lint clean (208 files), `bun test` 907 pass / 0 fail. Deviation: output path is `dist/features/plugins/recurrence/recurrence.*` not `dist/plugins/recurrence.*` (bunup cannot rename multi-entry outputs); `exports` kept consistent with the real path.

---

## Changes (Task F4)

- **plugins (dogfooding lock-in)**: Locked in the dogfooding boundary so future drift is caught — plugin IMPLEMENTATION source under `src/features/plugins/**` may import ONLY from the public API (`@ilamy/calendar`/`@ilamy/calendar/...`), bare npm packages, or relative paths, never deep internals via the `@/` alias.
- **import-graph test (primary enforcement)**: New `src/features/plugins/plugins-import-boundary.test.ts`. Recursively reads all non-test `*.ts`/`*.tsx` plugin source (`node:fs`/`node:path`), extracts import/re-export/side-effect specifiers via regex, and asserts none start with the internal `@/` alias. Violations are reported as `path -> 'specifier'`. Currently passes (recurrence source was cleaned in F3a).
- **biome override (secondary enforcement)**: Added a per-glob `overrides` entry in `biome.json` scoping `style/noRestrictedImports` (`level: error`) with a `patterns[].group: ["@/**"]` disallow + custom message to `src/features/plugins/**`, excluding `src/features/plugins/lib/**` and test/spec files. Biome 2.4.9 (installed; latest 2.4.16) supports this cleanly — confirmed against `node_modules/@biomejs/biome/configuration_schema.json`: `NoRestrictedImportsOptions.patterns` (gitignore-style) + `PatternOptions.group`/`message`, and `OverridePattern.includes` for per-glob scoping. Proven to fire: injecting `import { cn } from '@/lib/utils'` into a recurrence file produced the custom error; file restored byte-identical.

## Files Modified (Task F4)

- `src/features/plugins/plugins-import-boundary.test.ts` — New. 2 tests: (1) discovers >0 plugin source files; (2) no source file imports a `@/` internal.
- `biome.json` — New `overrides` entry: per-glob `noRestrictedImports` banning `@/**` in plugin source (excludes `lib/` + tests).

## Notes (Task F4)

SCOPE DECISION (both test and biome override): `src/features/plugins/lib/` is EXCLUDED from the boundary. It is NOT a plugin — it is the plugin SDK itself (`IlamyPlugin`/`PluginRuntime`/`PluginView` contract + `createPluginRuntime`). The calendar CORE consumes it (`use-calendar-engine.ts`, calendar context/provider) and `src/index.ts` re-exports its types as the PUBLIC plugin SDK. Forcing `lib/` to import from `@ilamy/calendar` would invert the dependency into a cycle (the package entry imports `lib/types`). F3a explicitly migrated recurrence source only; it never touched `lib/`. With `lib/` in scope the test caught 3 pre-existing internal imports that are correct for SDK machinery and would be wrong to "fix": `lib/types.ts` -> `@/components/types` (CalendarEvent) and `@/lib/configs/dayjs-config` (Dayjs/ManipulateType); `lib/create-plugin-runtime.ts` -> `@/lib/utils/event-utils` (`eventOverlapsRange`, which is NOT in the public API). Per the hard rule "report rather than hack around it," `lib/` source was left untouched and excluded from the boundary with a documented rationale in both the test header and the biome override. Test files remain exempt (they legitimately use `@/` internals). Tests written/run before reporting. Full suite: 909 pass / 0 fail (was 907, +2 boundary tests). Type-check clean. Lint clean (`bun run lint`). Biome `check` (incl. organize-imports) clean on touched files.

---

## Changes (Task G1)

- **docs**: Wrote `docs/writing-plugins.md` (third-party authoring guide) and `docs/migration-v2.md` (v1→v2 breaking-change guide). Documentation only — no source changes.

## Files Modified (Task G1)

- `docs/writing-plugins.md` — New. Third-party plugin authoring guide: IlamyPlugin contract, all seven capabilities (transformEvents, managesEvent, applyEdit/applyDelete, renderSlot, contribute/collect, views, provider), declaration merging, IlamyCalendarApi surface, packaging, complete worked example.
- `docs/migration-v2.md` — New. v1→v2 breaking-change guide: opt-in plugins, recurrence subpath move, CalendarEvent lean core, context method renames (updateRecurringEvent/deleteRecurringEvent → applyScopedEdit/applyScopedDelete, findParentRecurringEvent removed), CalendarView widened to string, exports encapsulation.

## Notes (Task G1)

All API names verified against source: `src/features/plugins/lib/types.ts`, `src/components/calendar-slots.ts`, `src/hooks/use-smart-calendar-context.ts`, `src/index.ts`, `package.json` exports map, and the recurrence plugin source. No source or test files modified. Type-check unaffected.
