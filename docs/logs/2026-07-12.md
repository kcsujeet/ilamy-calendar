# Development Log - 2026-07-12

## Changes

- **[tests/engine]**: Addressed code-review findings on PR #234 (test-only). Extracted the repeated engine + `onDateChange` spy setup in the three sequencing tests into a local `renderNavEngine(initialView)` helper (mirroring the file's `renderRecurrenceEngine` pattern), plus an `expectSecondEmissionDay(onDateChange, isoDay)` helper for the duplicated last-emission assertion block. Replaced `mock.calls[1]` bracket access with `mock.calls.at(1) ?? []` + optional-chained assertions per the code-style `.at()` rule.

- **[docs/website]**: Documented the post-v2.0.0 API changes on both doc surfaces. Website: added `onMoreEventsClick` to the calendar Event Handlers table (#230), a "More Events Overflow" section with example in customization/components, the `setView(view, date?)` signature + atomic-navigation note on the context page (#233/#234), a single-emission note on `onDateChange`, and fixed the `onViewChange` type (v1 view union → `CalendarView` string, which includes plugin views). Repo docs: corrected `dayMaxEvents` default in types-and-interfaces.md (`3` → `4`, actual `DAY_MAX_EVENTS_DEFAULT` in lib/constants.ts).

- **[release]**: Cut v2.0.1 — `onMoreEventsClick` prop (#230) plus the two navigation `onDateChange` fixes (#233 year-view atomic date+view, #234 latest-nav-state emissions).

- **[perf/bundle]**: Dropped the `motion` dependency, cutting the consumer bundle by 117.4kB min / 40.3kB gzip (~26-28%). Verified bundlephobia's claim empirically (esbuild consumer fixture + metafile, react/dayjs external): motion-dom + framer-motion were 123kB (28%) — real; lucide-react's reported 20.3% is a bundlephobia tree-shaking artifact (2.6kB / 0.6% in a real bundle — nothing to fix). All of motion entered through `AnimatedSection`; rewrote it as a CSS-only enter animation using the `tailwindcss-animate` peer-plugin utilities already used by the @ilamy/ui shadcn components (`animate-in fade-in slide-in-from-top-2.5`, `fill-mode-backwards` + inline `animationDelay` for stagger, `ease-in-out` = the previous cubic-bezier(0.4,0,0.2,1), remount on `transitionKey` replays). Intentional behavior losses (user-approved): the 150ms exit fade on view switches (AnimatePresence) and the drag-settle layout FLIP on events (`layout`/`layoutId` in draggable-event). Consumer bundle now 326.6kB min / 103.4kB gzip; top contributor is self dist (29.7%), then @dnd-kit/core (11.4%).

## Files Modified

- `packages/calendar/src/features/calendar/hooks/use-calendar-engine.test.ts` - `renderNavEngine` + `expectSecondEmissionDay` helpers; `.at(1)` access; added `CalendarView` type import
- `apps/website/src/content/docs/components/calendar.mdx` - `onMoreEventsClick` row, `onViewChange` type fix, `onDateChange` atomic note
- `apps/website/src/content/docs/components/use-ilamy-calendar-context.mdx` - `setView(view, date?)` signature
- `apps/website/src/content/docs/customization/components.mdx` - New "More Events Overflow" section
- `docs/types-and-interfaces.md` - `dayMaxEvents` default `3` → `4`
- `packages/calendar/src/components/animations/animated-section.tsx` - CSS-only rewrite; motion imports removed; `layout`/`layoutId` props dropped
- `packages/calendar/src/components/draggable-event/draggable-event.tsx` - Removed `layout`/`layoutId` usage
- `packages/calendar/package.json` - Removed `motion` dependency and keyword
- `bun.lock` - Lockfile update after dep removal

## Notes

- No production code changed. 72/72 engine tests pass, tsc clean, Biome clean.
- Deleted `2026-06-21.md` to respect the 10-file cap.
