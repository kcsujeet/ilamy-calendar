# Development Log - 2026-05-14

## Changes

- **[week-view]**: Week header column labels now use the calendar `translator` (`t('sun')`–`t('sat')`) instead of dayjs `format('ddd')`, which ignored custom translations.
- **[month-view]**: Month header weekday columns use the same translation keys and `firstDayOfWeek` ordering as week view.
- **[title-content]**: Month selector options and the displayed date range use `t()` for full and short month names instead of hardcoded dayjs formats.
- **[translations]**: Added short month keys (`jan`–`dec`, `mayShort`) to `Translations` and `default.ts` so title content can render locale-aware labels via the translator API.

## Files Modified

- `src/features/calendar/components/week-view/week-view.tsx` - `WEEKDAY_SHORT_TRANSLATION_KEYS`; header shows `t()` output with `capitalize`.
- `src/features/calendar/components/week-view/week-view.test.tsx` - Regression test asserting translator-provided weekday labels.
- `src/features/calendar/components/month-view/month-header.tsx` - Weekday headers routed through `t()` and `firstDayOfWeek`.
- `src/features/calendar/components/month-view/month-view.test.tsx` - Tests for translated weekday headers.
- `src/components/header/title-content.tsx` - Month picker and title date range use translation keys; browser locale drives date ordering/formatting.
- `src/components/header/base-header.test.tsx` - Tests for short-month translation in title content.
- `src/lib/translations/types.ts` - `jan`–`dec` / `mayShort` keys on `Translations`.
- `src/lib/translations/default.ts` - English defaults for short month names.

## Notes

Four commits on this branch ahead of `main` (`a0c48ad` → `8ed6035`). Remaining i18n gaps on the branch (2026-05-15): year-view month titles, weekday letters, and `firstDayOfWeek` in mini-calendars — see `docs/logs/2026-05-15.md`.
