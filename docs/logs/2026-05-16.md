# Development Log - 2026-05-16

## Changes

- **[title-content]**: Replaced `t(WEEKDAY_KEYS)` / `t(MONTH_SHORT_KEYS)` / manual `isDayFirstLocale` branching with native `Intl.DateTimeFormat` via `formatLocaleDate` and `formatLocaleDateRange`. Restores the intent of `day.format('dddd, MMM D')` (removed in 8ed6035) using browser locale instead of translation keys for date labels.
- **[date-locale-format]**: Added `formatLocaleDate` and `formatLocaleDateRange` helpers (`formatRange` when supported, fallback to two `format` calls).
- **[dev logs]**: Consolidated canonical `docs/logs/2026-05-15.md`; removed duplicate split log `2026-05-15-translation.md`.

## Files Modified

- `src/lib/utils/date-locale-format.ts` — `formatLocaleDate`, `formatLocaleDateRange`
- `src/lib/utils/date-locale-format.test.ts` — Intl helper tests
- `src/components/header/title-content.tsx` — Intl for day/week/month picker labels and popover titles
- `src/components/header/base-header.test.tsx` — day view title assertion uses `Intl` expected string
- `docs/logs/2026-05-15.md` — canonical May 15 log (week gutter, year-view i18n, scroll-to-now)

## Notes

- Month/week/day strings in the header dropdown now follow `currentLocale` from `IlamyCalendar` without requiring `MONTH_SHORT_KEYS` / `WEEKDAY_KEYS` in the translator.
- `day-view.tsx` still uses `t()` + `formatDayViewHeaderDate` — can be aligned with Intl in a follow-up.
- Follow-up: `ui/calendar.tsx` / `date-picker.tsx` not wired to `locale` in event form.

## Changes (continued)

- **[day-view]**: Header date uses `formatLocaleDate` (`weekday`/`month` long + `day`/`year` numeric) instead of `t(WEEKDAY_KEYS)` + `t(MONTH_KEYS)` + `formatDayViewHeaderDate`.
- **[year-view]**: Month titles use `formatLocaleDate` with `{ month: 'long' }` instead of `t(MONTH_KEYS)`.
- **[date-locale-format]**: Removed unused `MONTH_KEYS`, `MONTH_SHORT_KEYS`, `WEEKDAY_KEYS`, `isDayFirstLocale`, `formatDayViewHeaderDate` (only `formatLocaleDate` / `formatLocaleDateRange` remain).

## Files Modified (continued)

- `src/features/calendar/components/day-view/day-view.tsx`
- `src/features/calendar/components/day-view/day-view.test.tsx`
- `src/features/calendar/components/year-view/year-view.tsx`
- `src/features/calendar/components/year-view/year-view.test.tsx`
- `src/lib/utils/date-locale-format.ts`
- `src/lib/utils/date-locale-format.test.ts`
