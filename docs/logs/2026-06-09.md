# Development Log - 2026-06-09

## Changes

- **recurrence**: Fixed "this" scope delete leaving detached overrides visible after editing then deleting a modified occurrence.

## Files Modified

- `packages/recurrence/src/utils/recurrence-handler.ts` - Use `recurrenceId` for occurrence identity on delete; remove matching detached overrides; dedupe EXDATEs.
- `packages/recurrence/src/utils/crud-operations.test.ts` - Regression test for edit-then-delete on modified override.

## Notes

Deleting a modified instance ("this" scope) now removes the detached override from stored events and keys EXDATE by original occurrence (`recurrenceId`), not the override's moved start time.
