# Development Log - 2026-06-13

## Changes

- **[recurrence/types]**: Added a required `deleted: CalendarEvent[]` field to `PluginMutationResult` (rows to persist via `onEventDelete`) and widened `applyDelete` to `CalendarEvent[] | PluginMutationResult`, mirroring `applyEdit`.
- **[recurrence handler]**: `updateRecurringEvent` now returns `deleted` on every branch; the "all" edit reports the detached overrides it drops (Google-Calendar reset) so consumers can delete those stale rows. `deleteRecurringEvent` now returns a structured `PluginMutationResult` instead of a bare array, reporting real stored-row deltas per scope.
- **[calendar data hook]**: `applyScopedEdit` now also dispatches `deleted -> onEventDelete`. `applyScopedDelete` was rewritten to mirror the edit path: it dispatches `updated/added/deleted` from a structured result, falling back to the old single-`onEventDelete` path for bare arrays.

## Files Modified

- `packages/types/src/index.ts` - Added `deleted` to `PluginMutationResult`; widened `applyDelete` return type.
- `packages/recurrence/src/utils/recurrence-handler.ts` - `deleted` on all `updateRecurringEvent` returns; `deleteRecurringEvent` returns `PluginMutationResult` with per-scope deltas.
- `packages/calendar/src/features/calendar/hooks/use-calendar-data.ts` - `deleted` loop in `applyScopedEdit`; structured-result handling in `applyScopedDelete`; updated deps.
- `packages/recurrence/src/utils/recurrence-handler.test.ts` - delete wrapper + new tests for `deleted`/`updated` per scope.
- `packages/recurrence/src/utils/crud-operations.test.ts` - delete wrapper for array-shape assertions.
- `packages/calendar/src/features/calendar/hooks/use-calendar-engine.test.ts` - new tests: scope-all delete cascades `onEventDelete`; scope-this delete fires `onEventUpdate` for the base EXDATE.
- `docs/migration-v2.md` - documented the `deleted` field, `applyDelete` widening, and the scoped-delete behavior change.

## Notes

- `deleted` contents per scope (delete): this -> dropped override (or `[]`); following -> `[]`; all -> base + every override. `updated`: this -> base with new EXDATE; following -> terminated base; all -> `[]`.
- edit "all" now reports dropped detached overrides in `deleted` (was silently filtered out of `events`).
- Engine tests require a build first (recurrence package resolves `@ilamy/calendar` via built `dist`) and must run from `packages/calendar` for the happy-dom preload.
- The recurrence plugin's `applyDelete` needed no logic change — the structured result flows through unchanged once the type widened.
- `PluginMutationResult` is unreleased (introduced earlier in this same PR/branch), so making `deleted` required is non-breaking.

## Changes (PR #194 review follow-up)

- **[recurrence handler — edit "this"]**: Editing an already-detached stored override no longer fires a redundant no-op `onEventUpdate` on the base. The branch now computes `baseChanged = nextExdates !== existingExdates || baseEvent.uid !== seriesUid` and reports the base in `updated` only when it actually changed. In a consistent store (base already exdates the occurrence and carries the series uid) `updated` is just `[detachedOverride]`; first-time/inconsistent stores still report `[base, override]`.
- **[recurrence handler — edit "following"]**: Detached overrides of the series whose occurrence falls after the split were left orphaned in the store. Added an `isFollowingOverride` predicate (mirrors `isDetachedOverrideOfSeries`); the terminated base now drops exdates beyond `until`; the orphaned overrides are excluded from `events` and reported in `deleted`. Return is `{ events, updated: [terminatedEvent], added: [newSeriesEvent], deleted: followingOverrides }`.
- **[issue #156 regression pins]**: Added engine-level characterization tests asserting scoped recurring DELETE fires callbacks carrying the BASE id (never a generated `_0`): this → `onEventUpdate` base + exdate, no delete; following → `onEventUpdate` base + `until`, no delete; all → `onEventDelete` base. These pin already-correct behavior on HEAD and passed as-is (no bug revealed).

## Files Modified (PR #194 review follow-up)

- `packages/recurrence/src/utils/recurrence-handler.ts` - edit-"this" `baseChanged` gate; edit-"following" cascade of orphaned overrides + exdate cleanup.
- `packages/recurrence/src/utils/crud-operations.test.ts` - replaced the prior `[base, override]` test with the consistent-store case (override only) plus an inconsistent-store case; added the following-scope cascade test.
- `packages/calendar/src/features/calendar/hooks/use-calendar-engine.test.ts` - added the #156 following-delete pin (this/all pins already existed).
- `docs/migration-v2.md` - noted that #194 also addresses #156 (scoped delete reports real stored rows, base id never `_0`).

## Notes (PR #194 review follow-up)

- TDD: both behavior fixes were driven red-first. RED evidence — edit-"this" consistent-store test failed with `updated` length 2 (expected 1); following-cascade test failed with `deleted` empty (expected the two overrides). Both green after the handler changes.
- The new series in edit-"following" carries an rrule, so the `isFollowingOverride` filter (which requires `!e.rrule`) never matches it — the filter is applied after the push intentionally.

## Changes (PR #194 self-review refactors)

- **[recurrence handler — delete dispatch]**: Converted `deleteRecurringEvent`'s `switch (scope)` to a `scopedDeleteHandlers` lookup map (`deleteThisScope`/`deleteFollowingScope`/`deleteAllScope`), mirroring `updateRecurringEvent`'s `scopedUpdateHandlers`. Both functions now dispatch the same scope union the same way and share the single `Invalid scope` throw shape. Behavior-preserving; pinned by the existing per-scope `events`/`updated`/`added`/`deleted` assertions.
- **[recurrence handler — replaceStoredOverride]**: Changed from 5 positional args (three adjacent same-typed `CalendarEvent`) to a single `ReplaceStoredOverrideArgs` object param, matching the `ScopedUpdateContext` pattern. Removes the call-site transposition risk the types could not catch.
- **[engine tests — dedupe]**: Added a `renderRecurrenceEngine(events)` helper in `use-calendar-engine.test.ts` that mounts the engine with the recurrence plugin and fresh callback spies; migrated all 11 recurring-event tests to it. Cut fallow's flagged duplication from 275 lines (15 clone groups) to 154 (9). Residual clones are pre-existing patterns in touched files (nav-range, range-query, provider blocks past my hunks) and explicit act/assert bodies, left as-is (out of scope / readability).

## Changes (recurrence-handler split)

- **[recurrence utils — module split]**: Broke the 708-line `recurrence-handler.ts` into five focused modules under `utils/` along its existing seams: `floating-time.ts` (RRule floating-time conversion), `series-helpers.ts` (shared series primitives: `isRecurringEvent`, `getEventParentUID`, `findBaseEventIndex`, `getOccurrenceStartISO`, `getSeriesTerminationDate`, `addExdateToBaseEvent`), `generate-recurring-events.ts`, `update-recurring-event.ts`, and `delete-recurring-event.ts`. Dependency DAG: floating-time + series-helpers ← generate; series-helpers ← update, delete. No cycles. Deleted the original file (no re-export barrel, per no-barrel rule).
- **[importers]**: Repointed `index.ts`, `recurrence-plugin.tsx`, and the four util test files at the concrete new modules. Helpers crossing module boundaries gained `export` (were module-private `const`).
- **[tests]**: `git mv recurrence-handler.test.ts → recurring-mutations.test.ts` and renamed its top-level describe. No test files created; imports repointed.

## Files Modified (recurrence-handler split)

- Created `packages/recurrence/src/utils/{floating-time,series-helpers,generate-recurring-events,update-recurring-event,delete-recurring-event}.ts`; deleted `recurrence-handler.ts`.
- `packages/recurrence/src/index.ts`, `packages/recurrence/src/recurrence-plugin.tsx` - concrete import paths.
- `packages/recurrence/src/utils/{recurring-mutations,crud-operations,calendar-integration,recurrence-timezone}.test.ts` - import paths (+ rename of the first).

## Notes (recurrence-handler split)

- Behavior-preserving move verified by the full suite (164 recurrence + 786 calendar pass), all 6 packages type-check, build green, fallow dead-code 0 / complexity 0 / duplication unchanged.

## Changes (move view test harnesses out of the public view modules)

- **[views — remove test-only wrappers]**: Deleted the `DayView`/`WeekView`/`MonthView` thin wrappers (`() => <ViewRenderer view={spec} />`) from `day.tsx`/`week.tsx`/`month.tsx` (and their now-unused `ViewRenderer` imports). These were only ever imported by tests; production wires the views through their `PluginView` spec via `ViewRenderer`. Having both the component and the spec in the same module added API ambiguity. `YearView` is a real component and was left untouched.
- **[testing — new harness module]**: Added `src/testing/view-harnesses.tsx` exporting `DayView`/`WeekView`/`MonthView` for tests. Internal-only: NOT re-exported from the published `@ilamy/calendar/testing` entry (`testing/index.tsx`), so the public testing surface stays just `CalendarTestProvider`.
- **[tests]**: Repointed 12 imports across 10 view/business-hours test files to `@/testing/view-harnesses`.

## Files Modified (view test harnesses)

- Created `packages/calendar/src/testing/view-harnesses.tsx`.
- `packages/calendar/src/features/calendar/components/views/{day,week,month}.tsx` - removed wrapper + `ViewRenderer` import.
- 10 `*.test.tsx` files under `features/calendar/components/` - import path repointed.

## Notes (view test harnesses)

- Behavior-preserving (only test-render plumbing moved): full CI green, 164 + 786 tests pass.
- `check:fix` tried to strip two unrelated deliberate `// biome-ignore noConsole` comments in `ilamy-calendar.tsx` / `view-renderer.tsx`; reverted those to keep the change scoped.

## Changes (audit: trim test-only / dead / over-exported code)

Audited all `export`s in the bundled packages for symbols used only by tests, never used, or exported but used only in their own file. Findings actioned:

- **Test-only code moved to `testing/`**: `lib/utils/generator.ts` (`generateMockEvents`, no non-test importer incl. demo) → `src/testing/generator.ts`. Repointed the 3 view-test imports.
- **Testing dirs consolidated**: `features/calendar/testing/resource-test-fixtures.ts` → `src/testing/resource-test-fixtures.ts`, so all test utils live under one `testing/` dir (alongside `index.tsx`, `view-harnesses.tsx`, `generator.ts`). Repointed the 4 importers.
- **Dead code deleted**: `DAY_NUMBER_TO_WEEK_DAYS` (`lib/constants.ts`), zero references repo-wide.
- **Over-exported internals un-exported** (used only within their own file; not public API): `processBusinessHours` + 17 internal types — `IsBusinessHourOptions`, `BusinessHoursRange`, `CalendarDataParams`, `CalendarEngineConfig`, `CalendarEngineHandlers`, `CalendarNavigationParams`, `CalendarInteractionParams`, `ProcessedWeekEventsResult`, `CalendarConfigParams`, `SmartCalendarContextType`, `ScrollToTimeAxis`, `UseScrollToTimeOptions`, `HorizontalLayoutInput`, `VerticalLayoutInput`, `ScopedEventMutation`, `HorizontalGridEventsLayerProps`, `MonthlyMode`, `OpUnitType`.

## Notes (audit)

- Left as-is: `ui/` shadcn primitives (unused exports kept in sync with upstream); `getNthWeekdayOfMonth` / `exportToICalendar` (used in production in-file, additionally exported for direct unit tests); `CalendarTestProvider` / `DayView` (correct in `testing/`). `exportToICalendar`'s feature is live (`downloadICalendar` wired to the header button).
- Verified safe by `bun run type-check` (export-drops) + full CI green (164 + 786 tests, all builds). Detection was name-based; type-check is the backstop for the un-exports.

## Changes (agenda view plugin — issue #139)

- **[plugins layout]**: Relocated `packages/recurrence` → `packages/plugins/recurrence` and added `packages/plugins/agenda` (`@ilamy/calendar-agenda`), grouping plugin packages under `packages/plugins/`. Added `packages/plugins/*` to root workspaces.
- **[agenda plugin]**: New opt-in plugin exposed at `@ilamy/calendar/plugins/agenda`. `agendaPlugin({ window })` registers an `agenda` `PluginView` (component escape hatch, like year). `window` is `'month'` (default) or a number of days; drives `range` + `navigationStep`. The view fetches its own window via `getEventsForDateRange` (independent of the active view), groups by day (empty days skipped), repeats multi-day events per spanned day with a display-only `Day N/M` indicator, labels all-day events, and opens the event form on click. Dependency-free (no virtualization); windowed paging instead of infinite scroll.
- **[build order]**: Root `build` is now topologically ordered (leaf libs → plugins → calendar → demo). The calendar bundle resolves the plugin packages' `@ilamy/ui|utils` imports via those packages' dist, which their own `clean` builds wipe; the prior parallel `--filter '*'` build raced on that (a second plugin made it reliably fail).
- **[i18n]**: Added `agenda` ('Agenda') and `agendaNoEvents` ('No upcoming events') translation keys.
- **[demo]**: Registered `agendaPlugin()` alongside recurrence in `demoPlugins`.

## Files (agenda)

- `packages/plugins/agenda/**` - new package (factory, view, day-group, row, grouping helper, window helpers, tests, build/test config).
- `packages/calendar/src/plugins/agenda.ts` - subpath entry; `bunup.config.ts`, `package.json`, `tsconfig.json` - wiring.
- `packages/calendar/src/lib/translations/default.ts` - agenda keys.
- `packages/calendar/src/features/calendar/hooks/use-calendar-engine.test.ts` - registration + navigation integration tests.
- root `package.json` - workspaces glob + ordered build; `packages/calendar/tsconfig.json`, recurrence tsconfig - relocation paths.
- `docs/agenda-view.md` - usage docs.

## Notes (agenda)

- Decision trail in `docs/agenda-plugin-plan.md`: plugin (not built-in) because the header renders every `getViews()` entry with no visibility prop, so a plugin is the only opt-in path without new core API. Separate package per plugin (not one combined) to keep recurrence's rrule/augmentation isolated from the dep-free agenda.
- Multi-day `Day N/M` + per-day repetition confirmed against Google Calendar's Schedule view.
- Full CI green: agenda 15 + recurrence 164 + calendar 788 tests, all builds + type-checks.

## Changes (agenda PR #196 review fixes)

- **[public API]**: Exposed `onEventClick(event)` on the public `IlamyCalendarApi` (`useIlamyCalendarContext`). It surfaces the existing internal `handleEventClick` (runs the consumer's `onEventClick` callback, else opens the event for editing via `editEvent`). Additive/non-breaking. Needed because `openEventForm(event)` builds a blank new-event draft (ignores `id`/`title`), so a plugin view had no way to open an existing event for edit.
- **[agenda — event click]**: Row now calls `onEventClick(event)` (opens the real event for edit) instead of `openEventForm(event)` (which created a draft). New test asserts clicking opens the clicked event, not a draft.
- **[agenda — multi-day]**: Only all-day events repeat per day; timed events appear once on their start day (even across midnight). `Day N/M` badge gated on `allDay`. Helper test added for the timed-start-day case.
- **[agenda — color dot]**: Uses `event.backgroundColor` via `cn()` className + inline `style` (mirrors `draggable-event.tsx`), so it renders both class-name and CSS-value colors; was using the text-color field via inline style only.
- **[agenda — prop drilling]**: `AgendaEventRow` reads `t`/`timeFormat`/`onEventClick` from context directly; `AgendaDayGroup` reduced to a single `group` prop.
- **[agenda — naming]**: `timePattern` → `getTimePattern`. Re-added `@ilamy/ui` dep (now used by `cn`).

## Notes (PR #196 review fixes)

- Full CI green: agenda 16 + recurrence 164 + calendar 788; fallow gate green (dead code 0).

## Changes (agenda — scroll fix)

- **[agenda — scroll]**: Wrapped the agenda list in `<ScrollArea className="h-full">` (`@ilamy/ui/components/scroll-area`), mirroring `YearView`. The root was a plain `flex flex-col` with no height/overflow, so a long agenda overflowed the fixed-height calendar shell without scrolling. Re-added the `@ilamy/ui/components/*` tsconfig path for the import.
- **[agenda — day counter]**: Dropped `flex-1` from the event-row title so the multi-day `(Day N/M)` indicator packs right after the title (was stretched to the row's right edge, unlike Google). Wrapped the indicator in parentheses to match Google's Schedule view.

## Changes (agenda — current-time indicator, today highlight, dot color + architecture rule)

- **[architecture rule + hook]**: Added `.agents/rules/architecture.md` (shared components live in `@ilamy/ui`; features/packages shouldn't share logic with each other; don't widen a feature/core public API to share an internal). Updated the code-review skill (section F) with a "Package/feature boundaries" check. Added a `research-reminder.sh` PreToolUse hook (Write|Edit) nudging a codebase search for existing patterns before implementing.
- **[@ilamy/ui — shared CurrentTimeIndicator]**: Extracted a context-free, prop-driven `CurrentTimeIndicator` to `@ilamy/ui` (rangeStart/rangeEnd/now/axis + optional `render`; default red line; core dayjs only so it stays a leaf). `@ilamy/ui` now depends on `@ilamy/utils`.
- **[calendar — wrapper]**: `components/current-time-indicator.tsx` is now a thin wrapper that wires the consumer's `renderCurrentTimeIndicator` + `view` from context to the shared `@ilamy/ui` component. Grid call sites and the existing test are unchanged.
- **[public API]**: Exposed `renderCurrentTimeIndicator` on `IlamyCalendarApi` so plugin views (agenda) can render the indicator consistently.
- **[agenda]**: Today group treats the row as a full day and overlays the shared `CurrentTimeIndicator` (line at current-time %); today's date is highlighted (filled circle + primary weekday); the event dot uses the event color (`cn(backgroundColor || 'bg-blue-500', color)`, mirroring `draggable-event`). Removed the hand-rolled now-line helper.
- **[build]**: Root build reordered to `types,utils` → `ui` → plugins → calendar → demo (ui now depends on utils).

## Notes (indicator refactor)

- The shared indicator uses only core dayjs (`valueOf`, `diff`) so it type-checks against `@ilamy/utils`'s plain dist type; `@ilamy/ui` resolves `@ilamy/utils` via the package (not a source tsconfig path) to avoid `inferTypes` re-emitting a stray `dayjs.d.ts` into utils/src.
- Full CI green: agenda 16 + recurrence 164 + calendar 788; builds + all type-checks.

## Changes (demo — source HMR for library edits)

- **[demo — vite]**: `apps/demo/vite.config.ts` now resolves the `@ilamy/*` packages to SOURCE instead of the published dist, so editing the calendar / plugins / UI hot-reloads into the running demo. Two mechanisms: (1) `resolve.alias` maps each package *name* to its `src` dir (global, so every importer shares one instance — needed for the configured-dayjs singleton in `@ilamy/utils` and the single React context in `@ilamy/calendar`; word-boundary matching keeps `@ilamy/calendar` from capturing `@ilamy/calendar-agenda`); (2) `resolve.tsconfigPaths: true` resolves each package's internal `@/*` per-file via its own tsconfig (calendar's `@/` → calendar/src, demo's `@/` → demo/src). Dropped the old global `@` alias (it would hijack calendar's `@/`). Added `dedupe: ['react','react-dom']`.

## Files Modified (demo — source HMR)

- `apps/demo/vite.config.ts` - Source resolution for `@ilamy/*` (aliases + native tsconfigPaths) + react dedupe.

## Notes (demo — source HMR)

- Used Vite 8's built-in `resolve.tsconfigPaths` rather than the `vite-tsconfig-paths` plugin. Verified empirically: a `vite build` with either resolves all 2372 modules identically (calendar's per-package `@/` resolves correctly, so it is not a single-root tsconfig). No new dependency.
- Library *tsconfigs were left untouched* on purpose: the agenda/recurrence packages import `@ilamy/calendar` with no tsconfig path for it (they resolve the published package at runtime). The global vite alias unifies that to source for every importer, so there is no second calendar instance / context mismatch. This keeps the change entirely inside `apps/demo`.
- The demo's `tsc --noEmit` type-check is unchanged (still resolves `@ilamy/*` via dist), so it keeps validating against the published API surface.
- Vite config changes require a dev-server restart to take effect.

## Changes (unify today-date highlighting)

- **[@ilamy/ui — shared atoms]**: Added two context-free presentational atoms — `DayNumberBadge` (the today-number circle: `h-5 w-5` round, `text-xs font-medium`, `today` → `bg-primary text-primary-foreground`) and `WeekdayLabel` (`text-xs text-muted-foreground`, `today` → `text-primary`). Single source of truth for today-highlighting; callers compute `today` and pass extra layout className. Added both to `@ilamy/ui` bunup entry.
- **[calendar — day headers]**: Replaced the hand-rolled today circle + weekday text in `week.tsx`, `month.tsx`, `resource-week-vertical-day-header.tsx`, `resource-week-horizontal-day-header.tsx` with the shared atoms. One consistent `h-5 w-5` circle everywhere (was a mix of `h-5 w-5` / `h-6 w-6 p-1` / `w-6 h-6`); the resource-horizontal header dropped its `today && 'bg-primary/10 font-bold'` header-background tint (number highlight is enough).
- **[calendar — DayNumber]**: `DayNumber` (month grid cells) is now a thin wrapper that computes `today` + the `keys.dayNumber` test id, deferring the highlight to `DayNumberBadge`. Same split as `CurrentTimeIndicator`; keeps `keys`/`date-utils` out of `@ilamy/ui` and preserves the `day-number-*` test-id contract (relied on by grid-cell, all-day-row, and keys tests).
- **[agenda — day group]**: Uses the shared atoms; removed its bespoke `dayNumberBase`/`dayNumberClass` (was `size-8 text-lg`) for the consistent `h-5 w-5` badge.
- **[tests]**: Updated `resource-week-horizontal.test.tsx` today-highlight assertions from the removed `bg-primary/10`/`font-bold` header background to `header.querySelector('.bg-primary.text-primary-foreground')` (the number badge).

## Files Modified (unify today-date highlighting)

- `packages/ui/src/components/day-number-badge.tsx` - New shared today-number circle atom.
- `packages/ui/src/components/weekday-label.tsx` - New shared today-aware weekday label atom.
- `packages/ui/bunup.config.ts` - Added both atoms to the build entry.
- `packages/calendar/src/components/day-number.tsx` - Now delegates to `DayNumberBadge`.
- `packages/calendar/src/features/calendar/components/views/{week,month,resource-week-vertical-day-header,resource-week-horizontal-day-header}.tsx` - Use the shared atoms.
- `packages/plugins/agenda/src/components/agenda-day-group.tsx` - Uses the shared atoms.
- `packages/calendar/src/features/calendar/components/views/resource-week-horizontal.test.tsx` - Updated today-highlight assertions.

## Notes (unify today-date highlighting)

- Out of scope (distinct visual treatments, left as-is): `day.tsx` (full-date title + "Today" pill), `year-view.tsx` (whole-cell highlight with selected/event states), `title-content.tsx` (nav-popover "Today" pill), `ui/calendar.tsx` (shadcn mini-calendar).
- Verified: type-check (all packages), 788 calendar + 16 agenda tests, lint clean on touched files, full ordered build + demo build.

## Changes (collapse DayNumber wrapper)

- **[calendar — DayNumber removed]**: Deleted `day-number.tsx` (+ `day-number.test.tsx`) and inlined its single use into `grid-cell.tsx` as `<DayNumberBadge data-testid={keys.dayNumber(day)} today={isToday(day)}>{day.format('D')}</DayNumberBadge>`. The wrapper had become a one-caller pass-through over `DayNumberBadge`; the `day-number-*` test-id contract still holds (grid-cell sets it; `all-day-row.test` + `keys.test` still cover it).

## Files Modified (collapse DayNumber wrapper)

- `packages/calendar/src/components/grid-cell.tsx` - Inlined `DayNumberBadge` (added `isToday` import, dropped `DayNumber` import).
- `packages/calendar/src/components/day-number.tsx` - Deleted.
- `packages/calendar/src/components/day-number.test.tsx` - Deleted.

## Notes (collapse DayNumber wrapper)

- Calendar suite now 784 tests (was 788; the 4 DayNumber wrapper tests removed). Type-check + lint clean.

## Changes (merge today-highlight atoms into one DayLabel)

- **[@ilamy/ui — DayLabel]**: Replaced the two atoms (`DayNumberBadge` + `WeekdayLabel`) with a single `DayLabel` (`packages/ui/src/components/day-label.tsx`). It owns a `flex flex-col items-center` wrapper and stacks an optional weekday over the today-aware number badge. Props: `today`, `dayNumber`, `weekday?`, `reverse?`, `className?`, `data-testid?`.
  - **Order via CSS, not markup**: default is weekday-above-number; `reverse` flips it with `flex-col-reverse` (no JSX branching). month + agenda pass `reverse` (number on top); the week-family uses the default.
  - **Consistent classes**: weekday styling is baked in identically (`text-xs text-muted-foreground`, centered, truncate) — the per-site `uppercase` (agenda) and `sm:text-sm` (week) are gone, as are the `mx-auto`/`text-center`/`w-full` number/weekday overrides (the wrapper centers everything now). Call sites pass only `today`/`dayNumber`/`weekday?` (+ `reverse` for number-on-top, + `data-testid` for grid cells).
- **[call sites]**: `grid-cell`, `month`, `week`, both resource-week day headers, and the agenda day group now use `DayLabel`. Removed the old `day-number-badge.tsx` + `weekday-label.tsx` (and the interim `day-cell.tsx`); updated the `@ilamy/ui` bunup entry.

## Files Modified (DayLabel merge)

- `packages/ui/src/components/day-label.tsx` - New single component (replaces the two atoms).
- `packages/ui/bunup.config.ts` - Entry now lists `day-label.tsx`.
- `packages/calendar/src/components/grid-cell.tsx`, `.../views/{month,week,resource-week-vertical-day-header,resource-week-horizontal-day-header}.tsx`, `packages/plugins/agenda/src/components/agenda-day-group.tsx` - Use `DayLabel`.

## Notes (DayLabel merge)

- Verified: type-check (all packages), 784 calendar + 16 agenda tests, lint/format clean, full ordered build + demo build.

## Changes (DayLabel: drop `reverse` prop)

- **[@ilamy/ui — DayLabel]**: Removed the `reverse` boolean; it was redundant with the existing `className` passthrough on the flex-col wrapper. Number-on-top call sites (month, agenda) now pass `className="flex-col-reverse"`. Safe because `cn()` → tailwind-merge resolves the flex-direction conflict group (verified: `twMerge('flex flex-col items-center', 'flex-col-reverse')` → `'flex items-center flex-col-reverse'`, so the base `flex-col` is dropped rather than left to CSS source order). Props are now `today`, `dayNumber`, `weekday?`, `className?`, `data-testid?`.

## Files Modified (drop reverse)

- `packages/ui/src/components/day-label.tsx` - Removed `reverse` prop + doc update.
- `packages/calendar/src/features/calendar/components/views/month.tsx`, `packages/plugins/agenda/src/components/agenda-day-group.tsx` - Use `className="flex-col-reverse"`.

## Notes (drop reverse)

- Verified: type-check, 784 calendar + 16 agenda tests, full build + demo build.

## Changes (DayLabel: data-testid on root)

- **[@ilamy/ui — DayLabel]**: Moved the forwarded `data-testid` from the inner number badge to the root flex-col wrapper (conventional place for a component's test id). Safe: the only consumers of the `day-number-*` id are `all-day-row.test` (counts via `queryAllByTestId(/^day-number-/)`) and `keys.test` (string value) — no class assertions through it. Verified: type-check, 784 calendar tests, lint clean.

## Changes (today highlight in vertical resource gutter)

- **[calendar — vertical resource gutter]**: The left-column date labels in the vertical resource arrangements rendered plain `<span>` pairs with no today awareness. Replaced both `gutterColumn` `renderLabel`s with `DayLabel` so today highlights consistently: `month.tsx` (resource month vertical, number-on-top via `flex-col-reverse`) and `week.tsx` (resource week daily vertical, weekday-on-top). The gutter cell is already `flex flex-col items-center`, so `DayLabel` drops in directly.

## Files Modified (vertical resource gutter)

- `packages/calendar/src/features/calendar/components/views/month.tsx` - `renderLabel` → `DayLabel`.
- `packages/calendar/src/features/calendar/components/views/week.tsx` - `renderLabel` → `DayLabel`.

## Notes (vertical resource gutter)

- Verified: type-check + 784 calendar tests. Number now renders as the shared `h-5 w-5` badge (small, today-filled), matching the other resource headers.
