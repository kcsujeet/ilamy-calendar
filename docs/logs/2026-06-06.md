# Development Log - 2026-06-06

## Changes

- **[monorepo / Phase 2]**: Extracted `@ilamy/ui` (`packages/ui`) — the generic, self-contained shadcn primitives (badge, button, card, checkbox, dialog, input, input-group, label, popover, scroll-area, select, textarea) + `cn`. No barrel: each is a subpath export (`@ilamy/ui/components/<name>`, `@ilamy/ui/lib/utils`) via explicit bunup multi-entry; package uses `dts.inferTypes` (stock shadcn components have no explicit return types, so isolated-declarations would error) and excludes `bunup.config.ts` from its tsconfig. `cn` moved here; core's `@/lib/utils` re-exports it so the 43 core call sites are untouched. `calendar`/`date-picker`/`time-picker` stay in `@ilamy/calendar` (coupled to `useSmartCalendarContext` / dayjs-config / date-utils). Calendar now deps `@ilamy/ui` (`workspace:*`), with tsconfig paths (`@ilamy/ui/components/*` → `../ui/src/components/*`, `@ilamy/ui/lib/utils`) for dev resolution and `@ilamy/ui` externalized in its bunup build; 16 calendar files repointed `@/components/ui/*` → `@ilamy/ui/components/*`. Pruned 7 now-unused deps from calendar (the moved primitives' radix packages + class-variance-authority). Root `test` script gained `--if-present` (ui has no tests). Checkpoint green: root `bun run ci` passes (biome 222 files, 916 tests, both builds). Note: `@ilamy/ui` dist nests under `dist/src/...` (bunup didn't strip the `src/` prefix for these entries); exports map points there — cosmetic, revisit in Phase 4.
- **[monorepo / Phase 1]**: Converted the repo to a Bun workspace (`docs/monorepo-architecture{,-plan}.md`). Root `package.json` is now a private workspace root (`workspaces: ["packages/*","apps/*"]`) holding the shared toolchain devDeps + delegating scripts (`bun run --filter '*' …`). All library source moved via `git mv src → packages/calendar/src` (plus `bunup.config.ts`, `bunfig.toml`, test setup `happydom.ts`/`testing-library.ts`/`matchers.d.ts`/`bun-env.d.ts`, `vite.config.ts`, `scripts/`, `styles/`, `components.json`, `tsconfig.json`). `packages/calendar/package.json` carries the `@ilamy/calendar` library manifest (name/version unchanged, deps, exports incl. the recurrence subpath for now). Added `tsconfig.base.json` (shared opts); the package tsconfig extends it and keeps `@/*` + the `@ilamy/calendar` self-reference paths → `src` (needed while recurrence still lives inside calendar; removed in Phase 3). Root `bunfig.toml` pins `[install].linker = "hoisted"` (new Bun workspaces default to `isolated`). Fixed biome.json override globs `src/…` → `**/src/…` so the dayjs-config / demo / plugin-boundary exceptions still match at the new path. Demo kept inside `packages/calendar` for now (carved to `apps/demo` in a later phase). Checkpoint green: root `bun run ci` passes (biome clean, 916 tests, build emits the same artifacts).

- **[release]**: Cut v1.8.1 — the recurring scope-"all" reset fix (#151/#182) plus the previously-unreleased `isCellDisabled` prop (#180). Patch version by maintainer's call despite #180's breaking `onCellClick`/`CellInfo` change; the breaking change is called out with a prominent warning at the top of the changelog/release notes.
- **[recurrence] #151 / PR #176**: Scope-"all" recurring edit now RESETS the series (Google-Calendar semantics). Previously, editing a series with scope "all" left behind any pre-existing detached override of the same series, duplicating the occurrence alongside the reset base. The `case 'all':` branch now clears `exdates` on the base and removes same-series detached overrides via a named `isDetachedOverrideOfSeries` predicate. The submitted wall-clock time is anchored to the base event's original date (duration preserved when no end is submitted).
- **[v2 plugin architecture — implementation complete]**: Finished Phases A–G of `docs/v2-plugin-architecture-plan.md` (the fully-pluggable v2.0.0). Per-task detail is in `docs/logs/2026-06-01.md` (Tasks A1–G1). Net result: generic plugin contract (`transformEvents`, `managesEvent`, `applyEdit`/`applyDelete`, `renderSlot`, `contribute`, `views`, `provider`); generic scoped-mutation API (`applyScopedEdit`/`applyScopedDelete` + shared `useScopedEventMutation`); core `event-form` is recurrence-agnostic; lean core `CalendarEvent` with recurrence fields re-added via declaration merging; iCal export via `collect`; recurrence brings its own UI; plugins are opt-in (no default registration); curated public `IlamyCalendarApi`; `@ilamy/calendar/plugins/recurrence` subpath via bunup multi-entry + `exports` map; dogfooding enforced (recurrence imports only `@ilamy/calendar`, biome override + import-boundary test).
- **[v2 / fallow cleanup]**: Ran `fallow audit` on the v2 branch (no circular deps, no architecture-boundary violations). Removed the dead code it surfaced: the orphaned `RecurrenceEditOptions` type (+ its now-unused `Dayjs` import), the unreferenced `recurrence-editor/index.ts` barrel, and the unused `export` on `calculateViewRange`. Dead exports now 0.0%; 914 tests pass. Remaining fallow flags are pre-existing/false-positive (`bunup.config.ts`, `examples/*` deps).
- **[v2 / review fix]**: Pre-merge review of #183 surfaced one issue — `RecurrenceFormSection` dereferenced `event.start.toDate()` unguarded, which throws if the form's `selectedEvent` has no `start` (the old inline editor defaulted start, this path did not). Guarded it: anchor `dtstart` only when a start exists, otherwise pass the rule through unanchored. Added a co-located `recurrence-form-section.test.tsx` reproducing the crash (toggling recurrence on a start-less event) — red before the fix, green after. 914 tests pass.
- **[v2 / rebase]**: Rebased `feat/v2-plugin-architecture` onto `main` to incorporate the v1.8.1 release and the #151 fix. Git rename-detection carried the #151 scope-"all" reset into the relocated `src/features/plugins/recurrence/utils/recurrence-handler.ts`; `package.json` auto-merged to keep v1.8.1 + the plugin `exports` map.

- **[v2 / test dedup]**: Reduced duplicated test setup flagged by `fallow audit` (behavior-preserving, same test count). `provider.test.tsx` — added a local `makeDailyRecurringEvent(overrides?)` factory, collapsing the two repeated recurring-event literals. `calendar-integration.test.ts` — added a local `generate(event, start?, end?)` call wrapper (supplies `currentEvents: []` + full-range defaults), a `makeDailyEvent(overrides?)` factory for the daily-series literals, and an `expectInstanceStarts(result, startISOs)` assertion helper that collapses the per-index COUNT/UNTIL start-time checks into array assertions. `crud-operations.test.ts` — added a local `expectTerminatedUntil(result, baseId, untilISO)` helper for the two scope-"following" terminated-series assertions. Left untouched (distinct tests, abstraction would hurt readability): the `recurrence-edit-dialog.test.tsx` group (two `it`s asserting different title/option regexes) and the `crud-operations.test.ts` error-handling group (two tests exercising `updateRecurringEvent` vs `deleteRecurringEvent` with different arg shapes). `fallow` clone groups dropped 31 → 27 (introduced 8 → 4; remaining 4 are the two intentionally-skipped test groups plus the ignored `helpers.ts`/`lib` dogfooding group and the non-test hooks group). 916 tests pass; type-check + lint clean.
- **[v2 / dedup #2]**: `CalendarContextType` now `extends CalendarEngineReturn` instead of re-declaring its members (resolved the `currentLocale` optional/required mismatch by inheriting the engine's required `string` — it is always present at runtime). Single source of truth for the engine surface, so future engine additions no longer need a parallel edit in the context type. Duplication 33 → 31; type-check + 916 tests green.
- **[v2 / dedup]**: Reduced three duplication groups flagged by `fallow audit` (behavior-preserving). (1) Provider composition — extracted the identical plugin-provider `reduceRight` wrap from `calendar-context/provider.tsx` and `resource-calendar-context/provider.tsx` into a shared `composePluginProviders(providers, children)` helper in the plugin kernel (`src/features/plugins/lib/compose-plugin-providers.tsx`), with 2 co-located tests (empty → children unchanged; two providers wrap outermost-first). Both providers now call the helper. (3) recurrence-handler — extracted the shared "this"-scope EXDATE-append shape (compute `targetEventStartISO`, append to `baseEvent.exdates`, write back) into a small `addExdateToBaseEvent` helper used by both the edit and delete `case 'this':` branches; #151 scope-"all" reset behavior untouched. (2) SKIPPED — `CalendarContextType` is NOT a strict superset of `CalendarEngineReturn`: it declares `currentLocale?: string` (optional) while the engine returns `currentLocale: string` (required). Forcing `extends CalendarEngineReturn` would make `currentLocale` required (TS2430 if left optional), a signature change, so per instructions I left the type as-is rather than widen/narrow behavior. `fallow` clone groups dropped 35 → 33 (introduced 11 → 9); 916 tests pass (914 + 2 new); type-check + lint clean.

- **[v2 / review — nested-if + dense-condition fixes]**: A checklist-driven code-review pass on this branch surfaced readability hard-rule violations missed earlier. Flattened nested `if`s: `use-scoped-event-mutation.ts` `handleConfirm` (guard-return inside the `setDialogState` updater); `ical.ts` `recurrenceICalProperties` (`const rruleLine = event.rrule ? formatRRule(event.rrule) : ''` then one push); `event-form.tsx` `handleDelete` (early-return guard); `recurrence-handler.ts` EXDATE filter (dropped the `hasExdates` guard — `event.exdates?.includes(...) ?? false` is equivalent when absent). Named the dense conditions: `create-plugin-runtime.ts` (`const isPresent = node !== null && node !== undefined`), `recurrence-editor.tsx` (`const showSummary = Boolean(show && value)`, with `getDescription(value ?? null, t)` to keep the narrowing the inline `&&` previously gave). Behavior-preserving: 916 tests pass, type-check + lint clean. **Process fix:** the first review pass missed three of these because it trusted an Explore agent's "rule: none" instead of self-running the sweep; added a mandatory self-run mechanical-sweep block (with commands) to section F of the `code-review` skill so a subagent's "none" can never again satisfy a readability check.
- **[v2 / build fix — isolatedDeclarations]**: GitHub CI failed at `NODE_ENV=production bunx bunup` with TS9016 (`Either add an explicit type to the object or use full key: value syntax`) on `use-scoped-event-mutation.ts:59-63` — the exported `useScopedEventMutation` hook returned a shorthand object (`{ dialogState, openEditDialog, ... }`) with no explicit return type, which `isolatedDeclarations` dts emission can't infer without cross-file analysis (per TS 5.5 release notes). Added an explicit `ScopedEventMutation` interface and annotated the hook's return. Note: local bunup (same v0.16.31) only emits this as a WARN and still exits 0, so `bun run ci` passed locally while GitHub escalated it to an Error — that's why the earlier "CI green" check missed it. Production build now clean (0 diagnostics, exit 0); the other exported hooks (`useCalendarEngine`, etc.) already carry explicit return types. 916 tests pass, type-check clean.
- **[ci / local-vs-CI parity]**: Root-caused why GitHub CI failed but local `bun run ci` passed on the same commit: bunup only escalates dts warnings (e.g. TS9016) to a non-zero exit when it detects a CI environment (`CI=true` / `GITHUB_ACTIONS=true`, both auto-set by GitHub Actions); locally those vars are unset so the same warning stays a non-fatal WARN and the build exits 0. Verified empirically: reintroducing the warning gives `bunx bunup` exit 0 plainly but exit 1 under `CI=true`. bunup exposes no documented `failOnWarn`/strict flag, so pinned the `build` script to `CI=true NODE_ENV=production bunx bunup` — now `bun run build`/`bun run ci` fail on dts warnings locally exactly as GitHub does (confirmed: reintroduced warning → local `bun run build` exit 1). The CI workflow itself is unchanged (`bun run ci`).
- **[v2 / readability — inline conditions]**: Unpacked dense inline multi-clause conditions into named steps across the branch's source (consistent with the existing `isDetachedOverrideOfSeries` helper's style; the line-229 expansion filter and `isRecurringEvent` were already clear and left as-is). `recurrence-form-section.tsx` `.find`: `candidate`/`candidateUid`/`isSameSeries` + `Boolean(candidate.rrule)`. `recurrence-handler.ts`: `findBaseEventIndex` predicate → block with `belongsToSeries` + `isBaseSeries`; the overrides `.filter` → block with `isOverride` + `belongsToSeries`. `calendar-dnd-context.tsx`: the 3-clause update guard → a named `hasNoUpdates`. `recurrence-editor.tsx`: the `END_TYPES.map` arrow → a block exposing `showCountInput`/`showUntilInput` for the two JSX gates (was `type === 'count' && endType === 'count'` inline). Also cleared two pre-existing dead-code lint warnings in that file (unused `useMemo` import, unused `d` map param → `_weekday`). Behavior-preserving: 916 tests pass, type-check + lint clean. (Covered going forward by the code-review skill's "Named variables over inline expressions" rule.)
- **[v2 / readability — ternaries]**: Removed nested and multi-line ternaries from the branch's source per the code-style rule (single ternaries stay; nested or line-wrapped ones become named variables / early returns / lookup objects). `recurrence-editor.tsx`: the nested `endType` (`until ? : count ? : never`) → `if/else if`; the nested+wrapped `byweekday` → a module-level `resolveByweekday()` helper; `formatRecurrenceText` return and `toggleDay` → guard + single-line ternary; the inline `DatePicker onChange` arrow → a named `handleUntilChange`. `recurrence-plugin.tsx`: `transformEvents` flatMap and `contribute` → early-return blocks. `ical.ts` + `export-ical.ts` `formatDate`, `recurrence-handler.ts` `floatingUntil`, `recurrence-form-section.tsx` `parent`, `recurrence-edit-dialog.tsx` `title`/`question`, `use-calendar-engine.ts` `navigatePeriod`, `event-form.tsx` `disabledDateMatcher`, `demo-data.ts` `resources` → flat `if`/assignment forms. Left untouched: the recurrence `ui/` primitives (shadcn-derived, kept in sync with upstream), pre-existing files on `main` (out of this PR's scope), and the demo's `resolveSharedCalendarProps` `cond ? value : undefined` props (single non-nested ternaries that only wrap due to long identifiers; demo code). Behavior-preserving: 916 tests pass, type-check + lint clean. Also promoted the ternary rule in the `code-review` skill (`.agents/skills/code-review/SKILL.md`) to a Hard Rule so nested/multi-line ternaries are always flagged on review, with the remedy catalog and the `ui/`-primitive + unmodified-file exceptions.
- **[v2 / readability]**: Replaced the inline plugin-slot rendering expressions (a `dialog.isOpen && dialog.event && getEventManager(event)?.renderSlot?.(SLOT, {...} satisfies Ctx)` chain and a `renderSlot(SLOT, {...} satisfies Ctx)` call with an inline `as CalendarEvent` cast) with two small named host components in `calendar-slots.tsx`: `EventFormSlot` (`event`/`onChange` props) and `EventMutationScopeSlot` (`dialog`/`onResolve`/`onCancel` props). Each reads `renderSlot`/`getEventManager` from context itself rather than having the call sites thread them through, and the scope component owns the open/closed guard. This also DRYs the scope-dialog block that was duplicated verbatim in `event-form.tsx` and `calendar-dnd-context.tsx`. `event-form.tsx` now builds a named `draftEvent` + `mergePluginUpdates` and renders `<EventFormSlot .../>` / `<EventMutationScopeSlot .../>`; dropped `renderSlot` from its context selector (read inside the slot now). Renamed `calendar-slots.ts` → `.tsx` (all imports are extensionless, so transparent); exported `ScopedMutationDialogState` from `use-scoped-event-mutation.ts` for the new prop type. Behavior-preserving: 916 tests pass, type-check + lint clean.
- **[v2 / fallow config]**: Added `fallow.toml` to scope the audit to shipped library code, so the non-shipped false positives stop driving the verdict to `fail`. `ignorePatterns = ["examples/**"]` drops the 3 example host-apps' `@ilamy/calendar`/`tailwindcss-animate` "unused dependency" flags (standalone consumer apps fallow can't resolve to the workspace). `entry = ["bunup.config.ts"]` marks the build config as a root (additive to fallow's auto-detected package.json/plugin entries) so it isn't reported as an orphaned unused file. `ignoreDependencies = ["bun-plugin-tailwind"]` covers a dep loaded by Bun at build time via `bunfig.toml` (`plugins = [...]`), not a static import, so dead-code can't trace it. `[health].ignore` + `[duplicates].ignore` exclude `src/components/demo/**` (untested-by-design playground whose branching inflates CRAP). Result: dead-code 7 → 0, complexity 7 → 3 (the 3 remaining are inherited `EventForm`/`handleSubmit`/`openEventForm`, deliberately left), verdict `fail` → `warn`, exit 1 → 0. The 4 introduced duplication groups that remain are the previously-documented intentional ones (two distinct test groups, the dogfooded `cn` helper, the curated-API hooks pair). The shipped entry points (`src/index.ts`, the recurrence subpath) are left to auto-detection via package.json `exports` — not pinned in `entry`, which would disable the auto-detected roots.
- **[v2 / complexity]**: Split the demo `DemoPage` (`src/components/demo/demo-page.tsx`, fallow CRITICAL: cyclomatic 16 / cognitive 28 / CRAP 272) into focused sub-components — behavior unchanged (same props, same `plugins`, same rendered output, demo-only code). New files: `demo-custom-renderers.tsx` (the `createRenderEvent` factory, `renderCurrentTimeIndicator`, `renderHour` callbacks fallow flagged HIGH/MODERATE CRAP), `demo-data.ts` (module-level plugins/handlers/resources/`createResourceEvents`), `demo-resource-picker.tsx` (the resource-toggle Card), and `demo-calendar-display.tsx` (the calendar Card + `RegularCalendar`/`ResourceCalendar` branch components + a `resolveSharedCalendarProps` helper that resolves the demo toggles once). `DemoPage` is now the composition root (state + wiring only). Fallow before: 6 complexity findings, `DemoPage` cyclomatic 16 / cognitive 28 (critical). After: 7 findings (the one critical function became several moderate ones), `DemoPage` no longer flagged at all; demo findings are now `DemoCalendarDisplay` 6/5, `resolveSharedCalendarProps` 6/5, `renderEvent` 6/5 (all moderate) and `renderCurrentTimeIndicator` 8/6 (high, unchanged from baseline). All remaining demo flags are `crap`-only — inherent to untested demo code with any branching, not a threshold breach. 916 tests pass; type-check, lint, build clean.

## Files Modified

- `src/features/plugins/lib/compose-plugin-providers.tsx` (new) + `.test.tsx` (new): shared plugin-provider composition helper and its 2 tests.
- `src/features/calendar/contexts/calendar-context/provider.tsx` & `src/features/resource-calendar/contexts/resource-calendar-context/provider.tsx`: replaced the inline `reduceRight` wrap with `composePluginProviders(...)`; dropped the now-unused `as React.ReactNode` cast.
- `src/features/plugins/recurrence/utils/recurrence-handler.ts`: added `addExdateToBaseEvent`; both `case 'this':` branches (edit + delete) now call it.
- `recurrence-handler.ts` (`#151`): `case 'all':` writes the anchored base with `exdates: undefined` and returns the list filtered through `isDetachedOverrideOfSeries`.
- `crud-operations.test.ts` (`#151`): `makeDailySeries` / `makeOverride` factories; scope-"all" tests (single base row with `exdates` cleared, detached override removed, wall-clock time anchored, and the resurrection regression test).
- `src/components/demo/demo-page.tsx`: reduced to composition root (state + wiring); calendar branches, renderers, data, and resource picker delegated to new sub-components.
- `src/components/demo/demo-custom-renderers.tsx` (new): `createRenderEvent`, `renderCurrentTimeIndicator`, `renderHour`.
- `src/components/demo/demo-data.ts` (new): `demoPlugins`, event handlers, `demoResources`, `createResourceEvents`, `handleResourceEventClick`.
- `src/components/demo/demo-resource-picker.tsx` (new): `DemoResourcePicker` resource-toggle panel.
- `src/components/demo/demo-calendar-display.tsx` (new): `DemoCalendarDisplay` + `RegularCalendar`/`ResourceCalendar` branches + `resolveSharedCalendarProps`.

## Notes

- Resurrecting previously-deleted/overridden occurrences on an "all" edit is intentional, matching Google Calendar: a whole-series edit collapses the series back to a single base row with no exceptions.
- v1.8.1 released from `main` (890 tests on that line). The v2 branch runs 909 tests; verify green after the rebase.
- Incidental (v2 branch): `@radix-ui/react-select` is now unused (recurrence's editor went native `<select>`); follow-up cleanup candidate.
